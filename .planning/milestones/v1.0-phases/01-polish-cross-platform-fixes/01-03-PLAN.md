---
phase: 01-polish-cross-platform-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - README-SYNC

must_haves:
  truths:
    - "README roadmap checkboxes accurately reflect what is implemented"
    - "Phase 1 items that are done are checked, items not done are unchecked"
    - "Phase 2 and 3 items that are done are checked"
    - "Log path note uses platform-aware language, not hardcoded /tmp"
  artifacts:
    - path: "README.md"
      provides: "Accurate roadmap checkboxes and platform-aware log path note"
      contains: "[x] YubiKey detection"
  key_links: []
---

<objective>
Update README.md roadmap checkboxes to reflect actual implementation state and fix the hardcoded `/tmp/yubitui.log` reference to be platform-aware.

Purpose: The README roadmap shows most items unchecked despite being implemented. The log path note references `/tmp` which is Linux/macOS-specific but the code uses `std::env::temp_dir()`.
Output: Accurate README reflecting true project state.
</objective>

<context>
@.planning/PROJECT.md
@README.md
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README roadmap checkboxes and fix log path reference</name>
  <read_first>README.md, src/main.rs, src/app.rs, .planning/PROJECT.md</read_first>
  <files>README.md</files>
  <action>
1. Update the Roadmap section checkboxes based on what PROJECT.md confirms is implemented:

   **Phase 1: Core Functionality** (all done):
   - [x] Project structure
   - [x] YubiKey detection
   - [x] Basic dashboard UI
   - [x] PIN retry counter display
   - [x] GPG agent diagnostics

   **Phase 2: Key Management** (all done):
   - [x] View existing keys
   - [x] Import keys via PIV — change text to "Import keys to card (via GPG)"
   - [x] Import keys via OpenPGP — remove this line (redundant with above, the app uses gpg --edit-key not separate PIV/OpenPGP paths)
   - [x] Generate keys on-device
   - [ ] Key attribute configuration — this is NOT implemented

   **Phase 3: SSH Integration** (all done):
   - [x] SSH configuration wizard
   - [x] SSH agent integration
   - [x] Public key export
   - [ ] authorized_keys management — NOT implemented (app exports key for manual copy)

   **Phase 4: Advanced Features** (none done):
   - [ ] Touch policy configuration
   - [ ] Attestation support
   - [ ] Multiple YubiKey support
   - [ ] Backup/restore workflows

2. Fix the log path note on line 110. Change:
   `**Note**: When running in TUI mode, logs are written to `/tmp/yubitui.log` to avoid interfering with the display. Use `tail -f /tmp/yubitui.log` in another terminal to watch logs in real-time.`
   To:
   `**Note**: When running in TUI mode, logs are written to your system's temp directory (e.g., `/tmp/yubitui.log` on Linux/macOS, `%TEMP%\yubitui.log` on Windows) to avoid interfering with the display.`

3. Do NOT change any other sections of README.md. Preserve all other content exactly.
  </action>
  <verify>
    <automated>cd C:/Users/mbeha/code/yubitui && grep -c "\[x\]" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\[x\] YubiKey detection" README.md` returns 1 match
    - `grep "\[x\] Basic dashboard UI" README.md` returns 1 match
    - `grep "\[x\] PIN retry counter display" README.md` returns 1 match
    - `grep "\[x\] GPG agent diagnostics" README.md` returns 1 match
    - `grep "\[x\] Generate keys on-device" README.md` returns 1 match
    - `grep "\[x\] SSH configuration wizard" README.md` returns 1 match
    - `grep "\[x\] Public key export" README.md` returns 1 match
    - `grep "\[ \] Touch policy" README.md` returns 1 match (Phase 4 still unchecked)
    - `grep "temp directory" README.md` returns 1 match (platform-aware language)
    - `grep -c "/tmp/yubitui.log" README.md` returns 0 or 1 (only as an example, not as the definitive path)
    - `grep "%TEMP%" README.md` returns 1 match (Windows path mentioned)
  </acceptance_criteria>
  <done>README roadmap checkboxes match actual implementation state. Log path note is platform-aware.</done>
</task>

</tasks>

<verification>
- README.md Phase 1 checkboxes are all [x]
- README.md Phase 2 has [x] for implemented items, [ ] for Key attribute configuration
- README.md Phase 3 has [x] for implemented items, [ ] for authorized_keys management
- README.md Phase 4 items are all [ ]
- Log path note mentions temp directory and both Unix and Windows paths
</verification>

<success_criteria>
README.md roadmap section accurately reflects what has been built. No false claims of completion for unimplemented features. Log path guidance works for all platforms.
</success_criteria>

<output>
After completion, create `.planning/phases/01-polish-cross-platform-fixes/01-03-SUMMARY.md`
</output>
