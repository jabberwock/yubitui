---
phase: 03-advanced-yubikey-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/yubikey/openpgp.rs
  - src/yubikey/pin.rs
  - src/yubikey/piv.rs
  - src/yubikey/key_operations.rs
  - src/yubikey/detection.rs
  - src/ui/keys.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "cargo test passes with at least 15 unit tests covering all parser functions"
    - "All parser functions have tests for normal input, empty input, and malformed input"
    - "No test calls any external command (gpg, ykman) — all use string fixtures"
    - "Fingerprint slice operations in keys.rs never panic on short strings"
  artifacts:
    - path: "src/yubikey/openpgp.rs"
      provides: "Unit tests for parse_card_status"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/pin.rs"
      provides: "Unit tests for parse_pin_status"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/piv.rs"
      provides: "Unit tests for parse_piv_info"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/key_operations.rs"
      provides: "Unit tests for parse_ykman_openpgp_info"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/detection.rs"
      provides: "Unit tests for detect_model_from_version"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src/yubikey/openpgp.rs tests"
      to: "parse_card_status fn"
      via: "direct call with fixture string"
      pattern: "parse_card_status"
    - from: "src/yubikey/pin.rs tests"
      to: "parse_pin_status fn"
      via: "direct call with fixture string"
      pattern: "parse_pin_status"
    - from: "src/ui/keys.rs"
      to: "fingerprint field"
      via: "safe slice using .get() instead of index"
      pattern: "get\\(\\.\\.16\\)"
---

<objective>
Add unit tests for every pure-Rust parser function in the yubikey module. Currently there are zero tests. Parser functions must become `pub` to enable testing and future integration tests. Also fix the fingerprint slice panic risk in keys.rs where `&sig.fingerprint[..16]` can panic on short strings.

Purpose: Establish meaningful test coverage before adding new features in Plans 02-04. Tests validate parser correctness with fixture strings — no hardware or CLI tools needed.

Output: `cargo test` passes with 15+ tests covering all parser functions. Fingerprint display is safe for any string length.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced-yubikey-features/03-RESEARCH.md
@src/yubikey/openpgp.rs
@src/yubikey/pin.rs
@src/yubikey/piv.rs
@src/yubikey/key_operations.rs
@src/yubikey/detection.rs
@src/ui/keys.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make parser functions pub and add tests for openpgp, pin, piv, key_operations, and detection parsers</name>
  <files>src/yubikey/openpgp.rs, src/yubikey/pin.rs, src/yubikey/piv.rs, src/yubikey/key_operations.rs, src/yubikey/detection.rs</files>
  <read_first>src/yubikey/openpgp.rs, src/yubikey/pin.rs, src/yubikey/piv.rs, src/yubikey/key_operations.rs, src/yubikey/detection.rs, src/yubikey/mod.rs</read_first>
  <behavior>
    openpgp.rs - parse_card_status:
    - Full gpg --card-status output with ed25519 keys returns correct OpenPgpState (version, name, URL, 3 key fingerprints)
    - Empty string returns card_present=true, all keys None, no name
    - Output with "[none]" key lines returns keys as None
    - Output with "Key attributes ...: rsa4096 rsa4096 rsa4096" parses key_attributes correctly

    pin.rs - parse_pin_status:
    - "PIN retry counter : 3 3 0\n" returns user=3, admin=3, reset=0, nothing blocked
    - "PIN retry counter : 0 3 0\n" returns user_pin_blocked=true
    - "PIN retry counter : 3 0 0\n" returns admin_pin_blocked=true
    - "PIN retry counter : 0 0 0\n" returns both blocked
    - No matching line returns defaults (3/3/0, nothing blocked)
    - PinStatus::is_healthy() true when retries >= 2 and nothing blocked
    - PinStatus::needs_attention() true when user_pin_retries <= 1 or anything blocked

    piv.rs - parse_piv_info:
    - "Slot 9a:\n  Algorithm: RSA2048\nSlot 9c:\n  Algorithm: ECCP256\n" returns 2 slots
    - Empty string returns empty slots vec

    key_operations.rs - parse_ykman_openpgp_info:
    - Full ykman openpgp info output with SIG/ENC/AUT keys returns all three slots with algorithms and fingerprints
    - Input with empty slot sections returns None for all slots
    - Input with only SIG populated returns signature=Some, others=None

    detection.rs - detect_model_from_version:
    - Version major=5 returns YubiKey5
    - Version major=4 returns YubiKey4
    - Version major=3 returns YubiKeyNeo
    - Version major=1 returns Unknown
  </behavior>
  <action>
    1. In `src/yubikey/openpgp.rs`: Change `fn parse_card_status(output: &str) -> Result<OpenPgpState>` to `pub fn parse_card_status`. Add `#[cfg(test)] mod tests` block at end of file with 4 tests:
       - `test_parse_card_status_full`: Fixture with Version 3.4, Name "Test User", URL "https://example.com/key.asc", Key attributes "ed25519 cv25519 ed25519", three key fingerprints (Signature: "ABCD 1234 5678 9012 3456  7890 ABCD EF01 2345 6789", Encryption: "1111 2222 3333 4444 5555  6666 7777 8888 9999 0000", Authentication: "AAAA BBBB CCCC DDDD EEEE  FFFF 0000 1111 2222 3333"). Assert version, cardholder_name, public_key_url, all three keys Some with correct fingerprints.
       - `test_parse_card_status_empty`: Input "" => card_present=true, all keys None
       - `test_parse_card_status_none_keys`: Input with "Signature key .....: [none]" lines => keys are None
       - `test_parse_card_status_rsa`: Input with "Key attributes ...: rsa4096 rsa4096 rsa4096" => parse key_attributes correctly

    2. In `src/yubikey/pin.rs`: Change `fn parse_pin_status` to `pub fn parse_pin_status`. Add `#[cfg(test)] mod tests` with 7 tests:
       - `test_parse_pin_status_normal`: "PIN retry counter : 3 3 0\n" => user=3, admin=3, reset=0, not blocked
       - `test_parse_pin_status_user_blocked`: "PIN retry counter : 0 3 0\n" => user=0, user_pin_blocked=true
       - `test_parse_pin_status_admin_blocked`: "PIN retry counter : 3 0 0\n" => admin=0, admin_pin_blocked=true
       - `test_parse_pin_status_all_blocked`: "PIN retry counter : 0 0 0\n" => both blocked
       - `test_parse_pin_status_no_match`: "some unrelated output\n" => defaults 3/3/0
       - `test_is_healthy`: PinStatus{user_pin_retries:3, admin_pin_retries:3, reset_code_retries:0, user_pin_blocked:false, admin_pin_blocked:false}.is_healthy() == true
       - `test_needs_attention`: PinStatus{user_pin_retries:1, ...}.needs_attention() == true

    3. In `src/yubikey/piv.rs`: Change `fn parse_piv_info` to `pub fn parse_piv_info`. Add `#[cfg(test)] mod tests` with 2 tests:
       - `test_parse_piv_info_with_slots`: "Slot 9a:\n  Algorithm: RSA2048\nSlot 9c:\n  Algorithm: ECCP256\n" => 2 slots with names "9a" and "9c"
       - `test_parse_piv_info_empty`: "" => empty slots vec

    4. In `src/yubikey/key_operations.rs`: Change `fn parse_ykman_openpgp_info` to `pub fn parse_ykman_openpgp_info`. Add `#[cfg(test)] mod tests` with 3 tests:
       - `test_parse_ykman_openpgp_info_full`: Fixture:
         "OpenPGP version:            3.4\nApplication version:        5.2.7\nPIN tries remaining:        3\nReset code tries remaining: 0\nAdmin PIN tries remaining:  3\nSIG key:\n  Fingerprint: ABCDEF0123456789\n  Algorithm:   ed25519\nENC key:\n  Fingerprint: 1234567890ABCDEF\n  Algorithm:   cv25519\nAUT key:\n  Fingerprint: FEDCBA9876543210\n  Algorithm:   ed25519\n"
         Assert: signature=Some with algo "ed25519" and fp "ABCDEF0123456789", encryption=Some with algo "cv25519", authentication=Some with algo "ed25519"
       - `test_parse_ykman_openpgp_info_empty_slots`: "SIG key:\nENC key:\nAUT key:\n" => all None
       - `test_parse_ykman_openpgp_info_partial`: Only SIG has data => signature=Some, others=None

    5. In `src/yubikey/detection.rs`: Change `fn detect_model_from_version` to `pub fn detect_model_from_version`. Add `#[cfg(test)] mod tests` with 4 tests:
       - `test_detect_model_yubikey5`: Version{major:5, minor:2, patch:7} => Model::YubiKey5
       - `test_detect_model_yubikey4`: Version{major:4, minor:3, patch:0} => Model::YubiKey4
       - `test_detect_model_neo`: Version{major:3, minor:1, patch:0} => Model::YubiKeyNeo
       - `test_detect_model_unknown`: Version{major:1, minor:0, patch:0} => Model::Unknown

    6. Run `cargo test` and `cargo clippy -- -D warnings` to confirm all 20 tests pass.
  </action>
  <verify>
    <automated>cargo test 2>&1 | grep -E "test result|running" && cargo clippy -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/yubikey/openpgp.rs contains `pub fn parse_card_status`
    - src/yubikey/openpgp.rs contains `#[cfg(test)]` and `mod tests`
    - src/yubikey/openpgp.rs contains `fn test_parse_card_status_full`
    - src/yubikey/pin.rs contains `pub fn parse_pin_status`
    - src/yubikey/pin.rs contains `fn test_parse_pin_status_user_blocked`
    - src/yubikey/pin.rs contains `fn test_is_healthy`
    - src/yubikey/piv.rs contains `pub fn parse_piv_info`
    - src/yubikey/piv.rs contains `fn test_parse_piv_info_with_slots`
    - src/yubikey/key_operations.rs contains `pub fn parse_ykman_openpgp_info`
    - src/yubikey/key_operations.rs contains `fn test_parse_ykman_openpgp_info_full`
    - src/yubikey/detection.rs contains `pub fn detect_model_from_version`
    - src/yubikey/detection.rs contains `fn test_detect_model_yubikey5`
    - cargo test exits 0 with at least 15 tests passing
    - cargo clippy -- -D warnings exits 0
  </acceptance_criteria>
  <done>All 20 tests pass across 5 parser modules. Parser functions are pub. No external commands invoked. cargo clippy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix fingerprint slice panic risk in keys.rs</name>
  <files>src/ui/keys.rs</files>
  <read_first>src/ui/keys.rs</read_first>
  <action>
    In `src/ui/keys.rs`, there are 3 occurrences of `&sig.fingerprint[..16]` (and similar for enc and auth fingerprints) around lines 96, 109, 122 that will panic if the fingerprint string is shorter than 16 characters. Replace each with a safe slice:

    Replace every occurrence of the pattern:
    ```rust
    &sig.fingerprint[..16]
    ```
    with:
    ```rust
    sig.fingerprint.get(..16).unwrap_or(&sig.fingerprint)
    ```

    Do the same for `enc.fingerprint[..16]` and `auth.fingerprint[..16]` — all occurrences of unchecked fingerprint slicing in the file.

    Search the entire file for `fingerprint[..` to find all occurrences.

    Run `cargo clippy -- -D warnings` and `cargo build` to confirm clean.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -3 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/keys.rs does NOT contain `fingerprint[..16]` (unchecked slice)
    - src/ui/keys.rs contains `fingerprint.get(..16).unwrap_or` at least once
    - cargo build exits 0
    - cargo clippy -- -D warnings exits 0
  </acceptance_criteria>
  <done>All fingerprint slice operations in keys.rs use safe .get() access. No panic risk on short or empty fingerprint strings.</done>
</task>

</tasks>

<verification>
cargo test 2>&1 | grep -E "test result|running"
cargo clippy -- -D warnings 2>&1 | tail -3
</verification>

<success_criteria>
- cargo test passes with 20 unit tests across 5 parser modules
- All parser functions (parse_card_status, parse_pin_status, parse_piv_info, parse_ykman_openpgp_info, detect_model_from_version) are tested with normal, empty, and edge-case inputs
- No test requires hardware or CLI tools
- Fingerprint display in keys.rs uses safe slice access
- cargo clippy -- -D warnings is clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-yubikey-features/03-01-SUMMARY.md`
</output>
