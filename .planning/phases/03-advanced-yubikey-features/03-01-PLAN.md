---
phase: 03-advanced-yubikey-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/yubikey/openpgp.rs
  - src/yubikey/pin.rs
  - src/yubikey/piv.rs
  - src/yubikey/key_operations.rs
  - src/yubikey/detection.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "cargo test passes with at least 15 unit tests covering all parser functions"
    - "All parser functions have tests for normal input, empty input, and malformed input"
    - "No test calls any external command (gpg, ykman) — all use string fixtures"
  artifacts:
    - path: "src/yubikey/openpgp.rs"
      provides: "Unit tests for parse_card_status"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/pin.rs"
      provides: "Unit tests for parse_pin_status"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/piv.rs"
      provides: "Unit tests for parse_piv_info"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/key_operations.rs"
      provides: "Unit tests for parse_ykman_openpgp_info"
      contains: "#[cfg(test)]"
    - path: "src/yubikey/detection.rs"
      provides: "Unit tests for detect_model_from_version"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src/yubikey/openpgp.rs tests"
      to: "parse_card_status fn"
      via: "direct call with fixture string"
      pattern: "parse_card_status"
    - from: "src/yubikey/pin.rs tests"
      to: "parse_pin_status fn"
      via: "direct call with fixture string"
      pattern: "parse_pin_status"
---

<objective>
Add unit tests for every pure-Rust parser function in the yubikey module. Currently there are zero tests. Parser functions are private (`fn`) — they must become `pub(crate)` so inline `#[cfg(test)]` modules can test them (there is no lib.rs, so integration tests in `tests/` cannot access internal modules).

Purpose: Establish meaningful test coverage before adding new features in Plans 02-04. Tests validate parser correctness with fixture strings — no hardware or CLI tools needed.

Output: `cargo test` passes with 15+ tests covering all parser functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced-yubikey-features/03-RESEARCH.md
@src/yubikey/openpgp.rs
@src/yubikey/pin.rs
@src/yubikey/piv.rs
@src/yubikey/key_operations.rs
@src/yubikey/detection.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make parser functions pub(crate) and add tests for openpgp, pin, piv parsers</name>
  <files>src/yubikey/openpgp.rs, src/yubikey/pin.rs, src/yubikey/piv.rs</files>
  <read_first>src/yubikey/openpgp.rs, src/yubikey/pin.rs, src/yubikey/piv.rs</read_first>
  <behavior>
    - parse_card_status with full gpg --card-status output returns correct OpenPgpState fields
    - parse_card_status with empty string returns card_present=true, no keys, no name
    - parse_card_status with [none] keys returns None for each key slot
    - parse_card_status with ed25519 key_attributes parses correctly (not default rsa2048)
    - parse_pin_status with "PIN retry counter : 3 0 3" returns user=3, admin=0, reset=3 and admin_pin_blocked=true
    - parse_pin_status with "PIN retry counter : 0 3 0" returns user=0, user_pin_blocked=true
    - parse_pin_status with no matching line returns defaults (3/3/0)
    - parse_piv_info with "Slot 9a:" lines returns slot entries
    - parse_piv_info with empty string returns empty slots vec
  </behavior>
  <action>
    1. In `src/yubikey/openpgp.rs`: Change `fn parse_card_status` to `pub(crate) fn parse_card_status`. Add a `#[cfg(test)] mod tests` block with these tests:
       - `test_parse_card_status_full`: Use this fixture string (from real gpg --card-status output):
         ```
         Version ..........: 3.4
         Name of cardholder: Test User
         URL of public key : https://example.com/key.asc
         Key attributes ...: ed25519 cv25519 ed25519
         Signature key .....: ABCD 1234 5678 9012 3456  7890 ABCD EF01 2345 6789
         Encryption key.....: 1111 2222 3333 4444 5555  6666 7777 8888 9999 0000
         Authentication key: AAAA BBBB CCCC DDDD EEEE  FFFF 0000 1111 2222 3333
         ```
         Assert: version="3.4", cardholder_name=Some("Test User"), public_key_url=Some("https://example.com/key.asc"), all three keys are Some with correct fingerprints, key_attributes="ed25519"
       - `test_parse_card_status_empty`: Input "" => card_present=true, all keys None, no name, no URL
       - `test_parse_card_status_none_keys`: Input with "Signature key .....: [none]" lines => keys are None
       - `test_parse_card_status_rsa_default`: Input with "Key attributes ...: rsa4096 rsa4096 rsa4096" => key_attributes="rsa4096"

    2. In `src/yubikey/pin.rs`: Change `fn parse_pin_status` to `pub(crate) fn parse_pin_status`. Add `#[cfg(test)] mod tests`:
       - `test_parse_pin_status_normal`: Input "PIN retry counter : 3 3 0\n" => user=3, admin=3, reset=0, no blocked
       - `test_parse_pin_status_user_blocked`: Input "PIN retry counter : 0 3 0\n" => user=0, user_pin_blocked=true, admin not blocked
       - `test_parse_pin_status_admin_blocked`: Input "PIN retry counter : 3 0 0\n" => admin=0, admin_pin_blocked=true
       - `test_parse_pin_status_all_blocked`: Input "PIN retry counter : 0 0 0\n" => both blocked
       - `test_parse_pin_status_no_match`: Input "some unrelated output\n" => defaults user=3, admin=3, reset=0
       - `test_pin_status_is_healthy`: Construct PinStatus{user_pin_retries:3, admin_pin_retries:3, reset_code_retries:0, user_pin_blocked:false, admin_pin_blocked:false} => is_healthy()=true
       - `test_pin_status_needs_attention`: PinStatus with user_pin_retries=1 => needs_attention()=true

    3. In `src/yubikey/piv.rs`: Change `fn parse_piv_info` to `pub(crate) fn parse_piv_info`. Add `#[cfg(test)] mod tests`:
       - `test_parse_piv_info_with_slots`: Input "Slot 9a:\n  Algorithm: RSA2048\nSlot 9c:\n  Algorithm: ECCP256\n" => 2 slots, slot names "9a" and "9c"
       - `test_parse_piv_info_empty`: Input "" => empty slots vec
  </action>
  <verify>
    <automated>cargo test yubikey::openpgp::tests yubikey::pin::tests yubikey::piv::tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/yubikey/openpgp.rs contains `pub(crate) fn parse_card_status`
    - src/yubikey/openpgp.rs contains `#[cfg(test)]` and `mod tests`
    - src/yubikey/openpgp.rs contains `fn test_parse_card_status_full`
    - src/yubikey/pin.rs contains `pub(crate) fn parse_pin_status`
    - src/yubikey/pin.rs contains `fn test_parse_pin_status_user_blocked`
    - src/yubikey/piv.rs contains `pub(crate) fn parse_piv_info`
    - src/yubikey/piv.rs contains `fn test_parse_piv_info_with_slots`
    - cargo test exits 0 with all parser tests passing
  </acceptance_criteria>
  <done>All 11+ tests pass for openpgp, pin, and piv parsers. Parser functions are pub(crate). No external commands invoked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for key_operations parser and detection model logic</name>
  <files>src/yubikey/key_operations.rs, src/yubikey/detection.rs</files>
  <read_first>src/yubikey/key_operations.rs, src/yubikey/detection.rs, src/yubikey/mod.rs</read_first>
  <behavior>
    - parse_ykman_openpgp_info with full ykman output returns all three slots with algorithms and fingerprints
    - parse_ykman_openpgp_info with empty slot lines ("SIG key:\nENC key:\nAUT key:\n") returns no slots populated
    - parse_ykman_openpgp_info with partial data (only SIG populated) returns SIG Some, others None
    - detect_model_from_version with major=5 returns YubiKey5
    - detect_model_from_version with major=4 returns YubiKey4
    - detect_model_from_version with major=3 returns YubiKeyNeo
    - detect_model_from_version with major=1 returns Unknown
  </behavior>
  <action>
    1. In `src/yubikey/key_operations.rs`: Change `fn parse_ykman_openpgp_info` to `pub(crate) fn parse_ykman_openpgp_info`. Also change `fn save_slot` to `fn save_slot` (already private, keep as-is since tests call parse_ykman_openpgp_info which uses it internally). Add `#[cfg(test)] mod tests`:
       - `test_parse_ykman_openpgp_info_full`: Use fixture:
         ```
         OpenPGP version:            3.4
         Application version:        5.2.7
         PIN tries remaining:        3
         Reset code tries remaining: 0
         Admin PIN tries remaining:  3
         SIG key:
           Fingerprint: ABCDEF0123456789
           Algorithm:   ed25519
         ENC key:
           Fingerprint: 1234567890ABCDEF
           Algorithm:   cv25519
         AUT key:
           Fingerprint: FEDCBA9876543210
           Algorithm:   ed25519
         ```
         Assert: signature=Some with algo "ed25519" and fp "ABCDEF0123456789", encryption=Some with algo "cv25519", authentication=Some with algo "ed25519"
       - `test_parse_ykman_openpgp_info_empty_slots`: Input "SIG key:\nENC key:\nAUT key:\n" => all three are None (save_slot skips when both algo and fp empty)
       - `test_parse_ykman_openpgp_info_partial`: Input with only SIG populated => signature=Some, others=None

    2. In `src/yubikey/detection.rs`: Change `fn detect_model_from_version` to `pub(crate) fn detect_model_from_version`. Add `#[cfg(test)] mod tests`:
       - `test_detect_model_yubikey5`: Version{major:5,minor:2,patch:7} => Model::YubiKey5
       - `test_detect_model_yubikey4`: Version{major:4,minor:3,patch:0} => Model::YubiKey4
       - `test_detect_model_neo`: Version{major:3,minor:1,patch:0} => Model::YubiKeyNeo
       - `test_detect_model_unknown`: Version{major:1,minor:0,patch:0} => Model::Unknown

    3. Run `cargo clippy -- -D warnings` and `cargo test` to confirm everything passes.
  </action>
  <verify>
    <automated>cargo test yubikey::key_operations::tests yubikey::detection::tests -- --nocapture 2>&1 | tail -20 && cargo clippy -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/yubikey/key_operations.rs contains `pub(crate) fn parse_ykman_openpgp_info`
    - src/yubikey/key_operations.rs contains `fn test_parse_ykman_openpgp_info_full`
    - src/yubikey/key_operations.rs contains `fn test_parse_ykman_openpgp_info_empty_slots`
    - src/yubikey/detection.rs contains `pub(crate) fn detect_model_from_version`
    - src/yubikey/detection.rs contains `fn test_detect_model_yubikey5`
    - cargo test exits 0 with all tests passing
    - cargo clippy -- -D warnings exits 0
  </acceptance_criteria>
  <done>All 7+ tests pass for key_operations and detection parsers. Total test count is 18+. cargo clippy clean.</done>
</task>

</tasks>

<verification>
cargo test 2>&1 | grep -E "test result|running"
cargo clippy -- -D warnings 2>&1 | tail -3
</verification>

<success_criteria>
- cargo test passes with 18+ unit tests
- All parser functions (parse_card_status, parse_pin_status, parse_piv_info, parse_ykman_openpgp_info, detect_model_from_version) are tested with normal, empty, and edge-case inputs
- No test requires hardware or CLI tools
- cargo clippy -- -D warnings is clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-yubikey-features/03-01-SUMMARY.md`
</output>
