---
phase: 13-ui-polish
plan: 05
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - src/tui/snapshots/
autonomous: false
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04
  - POLISH-05
  - POLISH-06
  - POLISH-07
  - POLISH-08

must_haves:
  truths:
    - "All snapshot tests pass with updated expectations"
    - "cargo test passes 100% with zero failures"
    - "All screens visually match PIN Management standard when viewed in terminal"
  artifacts:
    - path: "src/tui/snapshots/"
      provides: "Updated snapshot files for all polished screens"
  key_links:
    - from: "src/tui/snapshots/"
      to: "insta::assert_display_snapshot!"
      via: "insta snapshot re-acceptance"
      pattern: "\\.snap$"
---

<objective>
Re-accept all snapshot tests and verify the full polish pass.

Purpose: Plans 01-04 changed compose() output on 9 screens, which will cause snapshot test failures. This plan re-accepts all snapshots, ensures cargo test passes 100%, and performs human verification of the visual result.

Output: All snapshots updated, cargo test green, user-verified visual quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ui-polish/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-accept all snapshot tests and ensure cargo test passes</name>
  <files>src/tui/snapshots/</files>
  <action>
1. **Run cargo test to identify all snapshot failures:**
   ```bash
   cargo test 2>&1
   ```
   This will show which snapshots need updating.

2. **Accept all pending snapshots:**
   ```bash
   cargo insta accept
   ```
   If `cargo insta` is not available, manually review and update each `.snap` file:
   - For each test that fails with "snapshot mismatch", the new snapshot is in a `.snap.new` file
   - Rename `.snap.new` to `.snap` to accept the new visual output

3. **Re-run full test suite to confirm 100% pass:**
   ```bash
   cargo test 2>&1
   ```
   Every single test must pass. If any test fails for a reason OTHER than snapshot mismatch (e.g., compilation error, logic bug), fix the root cause in the relevant screen file.

4. **Verify no compilation warnings on polished files:**
   ```bash
   cargo check 2>&1
   ```
   Address any unused import warnings from the widget changes (e.g., if DataTable was imported but a screen doesn't use it).

5. **Review snapshot output for quality:**
   Manually inspect the new snapshot content for each screen to confirm:
   - Buttons render with visible borders/highlighting (not as flat text)
   - DataTable renders with column headers and aligned rows
   - ProgressBar renders as a visual bar element
   - Markdown renders with headings and formatting
   - Status badges use consistent bracket notation throughout
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo test 2>&1 | tail -5</automated>
  </verify>
  <done>cargo test passes 100% with zero failures; all snapshots reflect the new polished widget output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of visual polish across all screens</name>
  <files>src/tui/snapshots/</files>
  <action>
Present the polished application to the user for visual verification. Run `cargo run -- --mock` and walk through all 9 screens.

Screens to verify:
- Dashboard (home): Buttons for navigation, bracket status badges
- Keys (1): DataTable for slots, Button actions
- Diagnostics (2): DataTable for checks, Button actions
- PIN Management (3): Unchanged reference standard
- PIV (5): DataTable for slots, Button actions
- Help (6): Markdown content rendering
- OATH (7): DataTable for credentials, ProgressBar countdown, Button actions
- FIDO2 (8): DataTable for passkeys, bracket badges, Button actions
- OTP (9): DataTable for slots, Button actions
- Glossary (?): Markdown content rendering
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo test 2>&1 | tail -3</automated>
  </verify>
  <done>User approves visual quality of all screens; all screens match PIN Management standard with Buttons, DataTables, consistent badges, and proper layout</done>
</task>

</tasks>

<verification>
```bash
cd /Users/michael/code/yubitui && cargo test 2>&1 | grep -E "test result|FAILED"
```
</verification>

<success_criteria>
- cargo test passes 100% — zero failures
- All 9 screens visually match PIN Management quality standard
- Buttons render with visible affordance (borders, highlighting)
- DataTables render with aligned columns and headers
- Status badges are consistent bracket notation across all screens
- Markdown content renders with proper headings and formatting
- User approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-polish/13-05-SUMMARY.md`
</output>
