---
phase: 05-native-card-protocol
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/yubikey/pin_operations.rs
  - Cargo.toml
autonomous: false
requirements: [NO-GPG-BIN-01, NO-YKMAN-BIN-01]

must_haves:
  truths:
    - "find_ykman() function is deleted — no ykman detection code remains"
    - "openpgp-card, card-backend-pcsc, yubikey crates removed from Cargo.toml"
    - "No remaining ykman subprocess calls anywhere in src/"
    - "No remaining gpg --card-status calls anywhere in src/"
    - "cargo build succeeds with clean dependency tree"
    - "All tests pass"
  artifacts:
    - path: "src/yubikey/pin_operations.rs"
      provides: "Cleaned pin_operations without find_ykman"
    - path: "Cargo.toml"
      provides: "Clean dependency list without unused crates"
  key_links:
    - from: "src/yubikey/pin_operations.rs"
      to: "compilation"
      via: "no dead code from removed find_ykman"
      pattern: "cargo build succeeds"
---

<objective>
Remove all vestiges of ykman and unused crates. Verify zero ykman/gpg-card-status references remain. Human-verify on real hardware.

Purpose: Clean break from external CLI dependencies. After Plans 01 and 02 replaced all functionality, this plan removes the dead code and unused Cargo dependencies per D-02.

Output: Clean codebase with no ykman references, no unused card crates, verified by grep audit and human hardware test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-native-card-protocol/05-CONTEXT.md
@.planning/phases/05-native-card-protocol/05-01-SUMMARY.md
@.planning/phases/05-native-card-protocol/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove find_ykman, unused crates, and audit for stale references</name>
  <files>src/yubikey/pin_operations.rs, Cargo.toml</files>
  <read_first>
    - src/yubikey/pin_operations.rs (find_ykman function and any remaining ykman references)
    - Cargo.toml (openpgp-card, card-backend-pcsc, yubikey crate entries to remove)
    - src/yubikey/card.rs (verify apdu_sw is there and pin_operations no longer has its own copy)
  </read_first>
  <action>
**pin_operations.rs cleanup:**

1. Delete `pub fn find_ykman() -> Result<PathBuf>` entirely (the function body that searches PATH and Windows fallback locations for the ykman binary).
2. Delete the `use std::path::PathBuf;` import if it was only used by find_ykman.
3. Ensure `factory_reset_openpgp()` does NOT call find_ykman — it was already rewritten to use native PC/SC APDUs in a prior phase. Verify it uses `pcsc::` directly.
4. Ensure the file uses `super::card::apdu_sw` (from Plan 01 migration) and does NOT have a local `fn apdu_sw` definition.

**Cargo.toml cleanup (per D-02):**

Run the following to remove unused crates:
```bash
cargo remove openpgp-card card-backend-pcsc yubikey
```

If any of these crates are not present (already removed or never added), the command will warn but not error. Verify with `cargo build`.

**Full audit — grep sweep for stale references:**

After the above changes, run:
```bash
grep -rn "find_ykman\|ykman" src/ --include="*.rs" | grep -v "//.*ykman\|#\[allow\|test\|YKMAN\|as_ykman_arg"
```

Any hits that are NOT in:
- Comments explaining history
- `as_ykman_arg()` method in touch_policy.rs (keep for backward compat, or remove if unused)
- Test fixtures

...must be investigated and removed.

Also audit for residual gpg --card-status calls:
```bash
grep -rn "card-status\|card_status" src/ --include="*.rs" | grep -v "//\|test\|parse_card_status\|#\[allow"
```

Any hits that are actual subprocess calls (not test fixtures or dead-code parsers) must be removed.

**as_ykman_arg() cleanup:** The `as_ykman_arg()` method on TouchPolicy is now dead code since ykman is removed. Add `#[allow(dead_code)]` if not already present, OR delete it entirely if no tests reference it. Check with: `grep -rn "as_ykman_arg" src/ --include="*.rs"`.

**Verify build:**
```bash
cargo build 2>&1
cargo clippy -- -D warnings 2>&1
cargo test --lib 2>&1
```
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo build 2>&1 | tail -5 && cargo clippy -- -D warnings 2>&1 | tail -10 && cargo test --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/yubikey/pin_operations.rs does NOT contain `fn find_ykman`
    - src/yubikey/pin_operations.rs does NOT contain `PathBuf` import (unless used elsewhere in file)
    - Cargo.toml does NOT contain `openpgp-card`
    - Cargo.toml does NOT contain `card-backend-pcsc`
    - Cargo.toml does NOT contain the standalone `yubikey` crate (note: "yubikey" may appear in package name "yubitui" — that's fine)
    - `grep -rn "find_ykman" src/ --include="*.rs"` returns no matches
    - `grep -rn "Command::new(\"ykman\")" src/ --include="*.rs"` returns no matches
    - `grep -rn '"--card-status"' src/ --include="*.rs"` returns no matches (outside of test fixtures / dead-code parsers with #[allow(dead_code)])
    - `cargo build` exits 0
    - `cargo clippy -- -D warnings` exits 0
    - `cargo test --lib` exits 0
  </acceptance_criteria>
  <done>find_ykman deleted, unused crates removed from Cargo.toml, zero ykman subprocess calls in src/, zero gpg --card-status subprocess calls in src/, all tests pass, clean build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 5 on real YubiKey hardware</name>
  <files>N/A</files>
  <read_first>
    - .planning/phases/05-native-card-protocol/05-01-SUMMARY.md
    - .planning/phases/05-native-card-protocol/05-02-SUMMARY.md
  </read_first>
  <action>
Present the following verification steps to the user. All automated work is complete at this point.
  </action>
  <verify>User confirms approval or reports issues</verify>
  <acceptance_criteria>
    - User confirms "approved" after testing on real hardware
    - All dashboard data (serial, firmware, PIN counters, fingerprints, touch policies) displays correctly
    - No ykman binary required on the test system
  </acceptance_criteria>
  <done>User types "approved" confirming the app works on real hardware without ykman installed.</done>
  <what-built>Complete Phase 5: all card operations now use native PC/SC APDUs. No ykman or gpg --card-status subprocess calls remain. The app should detect YubiKeys, show card status, PIN retry counters, key attributes, touch policies, PIV slots, and attestation — all without ykman installed.</what-built>
  <how-to-verify>
    1. Run `cargo build --release` — should succeed with no warnings
    2. Run `cargo test --lib` — all tests should pass
    3. Insert a YubiKey and run the app: `cargo run --release`
    4. Verify the dashboard shows: serial number, firmware version, PIN retry counters, key fingerprints, touch policy per slot
    5. If you have a key with touch policy set, verify it shows correctly
    6. Navigate to key attributes screen — should show algorithm and fingerprint info
    7. Try setting touch policy on a slot (if comfortable) — verify the APDU-based set works
    8. Run `grep -rn "Command::new(\"ykman\")" src/` — should return zero results
    9. Run `grep -rn '"--card-status"' src/` — should return zero results (excluding test/dead-code parsers)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found during testing</resume-signal>
</task>

</tasks>

<verification>
- `cargo build` exits 0
- `cargo clippy -- -D warnings` exits 0
- `cargo test --lib` exits 0
- `grep -rn "find_ykman" src/ --include="*.rs"` returns 0 lines
- `grep -rn 'Command::new("ykman")' src/ --include="*.rs"` returns 0 lines
- `grep -rn '"--card-status"' src/ --include="*.rs"` returns 0 lines (outside dead-code parsers)
</verification>

<success_criteria>
- find_ykman deleted from pin_operations.rs
- openpgp-card, card-backend-pcsc, yubikey crates removed from Cargo.toml
- Full grep audit confirms zero ykman subprocess calls and zero gpg --card-status subprocess calls in src/
- Clean build, clean clippy, all tests pass
- Human verified: app works on real YubiKey hardware without ykman installed
</success_criteria>

<output>
After completion, create `.planning/phases/05-native-card-protocol/05-03-SUMMARY.md`
</output>
