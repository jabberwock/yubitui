---
phase: 05-native-card-protocol
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/yubikey/gpg_status.rs
  - src/yubikey/key_operations.rs
  - src/app.rs
autonomous: true
requirements: [NATIVE-PCSC-01]
gap_closure: true

must_haves:
  truths:
    - "Key import failure message distinguishes wrong Admin PIN from card disconnect"
    - "[V] View Card Status navigates to KeyOperationResult screen after fetching status"
    - "[E] Export SSH shows SshPubkeyPopup (not silent return) when no auth key present"
    - "Navigating to [V], [K], [E], [S], [A] from KeyScreen::Main clears stale message"
  artifacts:
    - path: "src/yubikey/gpg_status.rs"
      provides: "ScOpFailure(6) mapped to 'Wrong Admin PIN', CardCtrl(3) surfaced on failure"
    - path: "src/yubikey/key_operations.rs"
      provides: "CardCtrl(3) captured and included in error messages on import failure"
    - path: "src/app.rs"
      provides: "ViewStatus routes to KeyOperationResult; ExportSSH error routes to SshPubkeyPopup; message cleared in [V][K][E][S][A] nav arms"
  key_links:
    - from: "run_keytocard_session (key_operations.rs)"
      to: "status_to_message (gpg_status.rs)"
      via: "ScOpFailure(6) match arm"
      pattern: "ScOpFailure\\(6\\)"
    - from: "execute_key_operation (app.rs)"
      to: "KeyScreen::KeyOperationResult"
      via: "ViewStatus Ok branch routing"
      pattern: "KeyScreen::KeyOperationResult"
---

<objective>
Fix key import error reporting and three Key Management navigation bugs.

Purpose: After UAT, these are the highest-severity issues — the import blocker makes the core key import workflow unusable, and the navigation bugs make View Card Status and Export SSH silently fail.
Output: Working import error messages; [V] shows full card status; [E] shows error popup; stale messages cleared.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-native-card-protocol/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ScOpFailure(6) mapping and CardCtrl(3) surfacing in import</name>
  <files>src/yubikey/gpg_status.rs, src/yubikey/key_operations.rs</files>
  <action>
In src/yubikey/gpg_status.rs, update the `status_to_message` match for `ScOpFailure`:
- Add explicit arm `GpgStatus::ScOpFailure(6) => "Wrong Admin PIN".to_string()` BEFORE the catch-all `ScOpFailure(_)` arm.
- The catch-all `ScOpFailure(_)` already returns "Smartcard operation failed" — leave it.
- `CardCtrl(3)` already maps to "Card removed -- reinsert and retry" in status_to_message.

In src/yubikey/key_operations.rs, in `run_keytocard_session`, the `CardCtrl(_)` arm at ~line 568 currently suppresses all CardCtrl events silently. Change it to:
- Match `GpgStatus::CardCtrl(3)` specifically: call `status_to_message` and push the message to `messages`.
- Leave other `CardCtrl(_)` events suppressed (they are scdaemon connection noise between sessions).

This means a CardCtrl(3) (card disconnected) during import will appear in the returned messages alongside any ScOpFailure, giving the user both signals.
  </action>
  <verify>
cargo clippy -- -D warnings passes.
cargo test passes.
Manually trigger wrong Admin PIN import attempt and confirm message contains "Wrong Admin PIN" or "Card removed".
  </verify>
  <done>
ScOpFailure(6) returns "Wrong Admin PIN" from status_to_message. CardCtrl(3) during import produces a message in the output list. cargo test and cargo clippy both pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix [V] ViewStatus route, [E] ExportSSH error route, and stale message clear</name>
  <files>src/app.rs</files>
  <action>
Three targeted fixes to execute_key_operation and the KeyScreen::Main input handler in src/app.rs.

**Fix 1 — [V] ViewStatus routes to KeyOperationResult (lines ~931-946):**
In `execute_key_operation`, the `KeyScreen::ViewStatus` match arm currently routes both Ok and Err to `KeyScreen::Main`. Change to:
- Ok branch: after setting `key_state.message = Some(msg)`, set `key_state.screen = KeyScreen::KeyOperationResult` (not Main).
- Err branch: keep routing to `KeyScreen::Main` with the error message — error display in Main is fine.

**Fix 2 — [E] ExportSSH error routes to SshPubkeyPopup (lines ~955-957):**
In `execute_key_operation`, the `KeyScreen::ExportSSH` Err arm currently sets `key_state.screen = KeyScreen::Main`. Change it to `KeyScreen::SshPubkeyPopup`. The SshPubkeyPopup renderer already handles `None ssh_pubkey` with "No authentication key found on card." — so setting `ssh_pubkey = None` (which happens naturally on Err since Ok path sets it) means the popup will show the correct message.
Also ensure `key_state.ssh_pubkey = None` is set in the Err arm before routing to SshPubkeyPopup.

**Fix 3 — Clear stale message on [V][K][E][S][A] navigation (KeyScreen::Main handler):**
In the `KeyScreen::Main` input match block (~lines 219-295):
- `KeyCode::Char('v')` arm: add `self.key_state.message = None;` before setting screen to ViewStatus.
- `KeyCode::Char('k')` arm: add `self.key_state.message = None;` before setting screen to KeyAttributes.
- `KeyCode::Char('e')` arm: add `self.key_state.message = None;` before setting screen to ExportSSH.
- `KeyCode::Char('s')` arm: add `self.key_state.message = None;` before setting screen to SshPubkeyPopup.
- `KeyCode::Char('a')` arm (attestation): add `self.key_state.message = None;` at entry.

Note: [G] (keygen) and [T] (touch policy) already clear message — do not modify them.
  </action>
  <verify>
cargo clippy -- -D warnings passes.
cargo test passes.
With app running:
1. Press [V] after triggering an attestation error — confirm no stale error message bleeds through.
2. Press [V] with a key on card — confirm KeyOperationResult screen appears with card status text.
3. Press [E] with no auth key on card — confirm SshPubkeyPopup appears with "No authentication key found" message.
  </verify>
  <done>
[V] success shows KeyOperationResult. [E] error shows SshPubkeyPopup. Navigation to [V][K][E][S][A] clears prior message. cargo clippy and cargo test pass.
  </done>
</task>

</tasks>

<verification>
cargo build --release passes.
cargo clippy -- -D warnings passes.
cargo test passes (all 85+ tests).
</verification>

<success_criteria>
- ScOpFailure(6) → "Wrong Admin PIN" in status_to_message
- CardCtrl(3) produces a message entry during import failure
- [V] routes success to KeyScreen::KeyOperationResult
- [E] routes error to KeyScreen::SshPubkeyPopup with None ssh_pubkey
- [V][K][E][S][A] navigation arms each clear key_state.message
</success_criteria>

<output>
After completion, create `.planning/phases/05-native-card-protocol/05-04-SUMMARY.md`
</output>
