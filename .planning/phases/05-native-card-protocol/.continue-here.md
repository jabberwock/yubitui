---
phase: 05-native-card-protocol
plan: 03
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-03-26T09:27:22.932Z
---

<current_state>
Key import flow is working (SIG ✓ ENC ✓ AUT —). One remaining bug: fingerprint
detection shows "Keys: ❌ Sign ❌ Encrypt ❌ Auth" on dashboard even when keys are
on card. GET DATA 0xC5 returns SW 0x6B00 (Wrong P1-P2) in the detection path despite
exclusive mode + scdaemon killed. Root cause not yet found.
</current_state>

<completed_work>

- DELETE_KEY stub-check: only call DELETE_KEY when key file is shadow stub (contains
  "(shadowed") — prevents deleting real private key material before import
- DELETE_KEY --force: suppresses Pinentry confirmation dialog for card-referenced keys
- WaitForResult success: mark success=true when keyedit.prompt reached without
  SC_OP_FAILURE (gpg 2.4.9 doesn't emit SC_OP_SUCCESS for subkey keytocard)
- CardCtrl noise suppressed: "Card removed -- reinsert and retry" no longer shown
  in import result UI
- Detection reordered: reads OpenPGP DOs before management AID query
- Detection exclusive mode: always kills scdaemon before detection; always restarts after
- All committed in 50c065a
</completed_work>

<remaining_work>

- **BUG: GET DATA 0xC5 returns SW 0x6B00** — fingerprints read as zeros → "Keys: Not set"
  even when SIG+ENC are on card (confirmed via `gpg --card-status` and scdaemon)
- Task 2: Hardware verification gate (blocked on above)
- 05-03-SUMMARY.md
- Phase 5 VERIFICATION.md
- User's broader request: match ykman + YubiKey auth + all GPG features
</remaining_work>

<decisions_made>

- Exclusive mode always (not shared-first) for detection — shared mode causes C5 0x6B00
- OpenPGP DOs read before management AID SELECT — management AID SELECT corrupts session state
- These two changes together should fix C5 but still don't. Root cause unknown.
</decisions_made>

<blockers>

- **SW 0x6B00 for GET DATA 0xC5**: card rejects the APDU `[00 CA 00 C5 00]` with
  "Wrong P1-P2". Happens in detection path only. `get_key_attributes()` in
  key_operations.rs reads C5 via `connect_to_openpgp_card()` — does it work there?
  If yes, the difference is the sequence of APDUs before C5.

  **Debug approach for next session:**
  1. Add eprintln! debug BEFORE and AFTER each GET DATA call in detection path
  2. Confirm which specific GET DATA call first triggers the bad state
  3. Check if touch_policy::get_touch_policies_native (called before detection was
     reorganized to call it after) might be leaving card in bad state
  4. Try reading C5 immediately after SELECT OpenPGP with NO other APDUs first
</blockers>

<context>
Test key: DAB13513860C280ED85C6888A123FF7475998643
Admin PIN: 77113222 | User PIN: 123456 | GPG passphrase: Hpvu3sCHWKYdCFQNMzAv
Card serial: 26928089 | SIG + ENC on card, AUT not set
</context>

<next_action>
Start with: Add minimal eprintln! debug to detection.rs to find which APDU before C5
causes SW 0x6B00. Suspect: touch_policy::get_touch_policies_native or pin status read.
Try: read C5 as the very first GET DATA after SELECT OpenPGP (before anything else).
</next_action>
