---
phase: 01-polish-cross-platform-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.rs
  - src/ui/mod.rs
  - src/ui/help.rs
autonomous: true
requirements:
  - HELP-SCREEN

must_haves:
  truths:
    - "Pressing ? on the Dashboard shows a help screen with all keybindings"
    - "Help screen lists navigation keys (1-5), per-screen actions, and global keys (r, q, Esc)"
    - "Pressing Esc or ? again returns to the previous screen"
    - "Status bar updates to show 'Help' when on help screen"
  artifacts:
    - path: "src/ui/help.rs"
      provides: "Help screen render function"
      contains: "pub fn render"
    - path: "src/app.rs"
      provides: "Screen::Help variant, ? key handler, Esc from help returns to previous screen"
      contains: "Screen::Help"
    - path: "src/ui/mod.rs"
      provides: "help module declaration, Help in status bar match"
      contains: "mod help"
  key_links:
    - from: "src/app.rs"
      to: "src/ui/help.rs"
      via: "Screen::Help triggers ui::help::render"
      pattern: "ui::help::render"
    - from: "src/app.rs"
      to: "src/app.rs"
      via: "previous_screen field stores screen to return to"
      pattern: "previous_screen"
---

<objective>
Wire up the `?` key to show a help screen with a complete keybinding reference. The README documents `?` as showing help, but no handler or screen exists.

Purpose: Users expect `?` to work as documented. The help screen provides discoverability for all keyboard shortcuts.
Output: New help screen module, `?` key wired in app event loop, status bar updated.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app.rs
@src/ui/mod.rs
</context>

<interfaces>
<!-- From src/app.rs -->
```rust
pub enum Screen {
    Dashboard, Diagnostics, Keys, PinManagement, SshWizard,
}

pub struct App {
    should_quit: bool,
    current_screen: Screen,
    // ...
}
```

<!-- From src/ui/mod.rs — status bar -->
```rust
pub fn render_status_bar(frame: &mut Frame, area: Rect, app: &App) {
    let status_text = match app.current_screen() {
        Screen::Dashboard => "Dashboard",
        Screen::Diagnostics => "Diagnostics",
        // ...
    };
    let help_text = match app.current_screen() {
        Screen::Dashboard => "1-5: Switch View | R: Refresh | Q: Quit",
        _ => "ESC: Back | R: Refresh | Q: Quit",
    };
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create help screen module and add Screen::Help variant</name>
  <read_first>src/app.rs, src/ui/mod.rs, src/ui/keys.rs (for render pattern reference)</read_first>
  <files>src/ui/help.rs, src/ui/mod.rs, src/app.rs</files>
  <action>
1. Create `src/ui/help.rs` with a `pub fn render(frame: &mut Frame, area: Rect)` function that renders a keybinding reference. Use a ratatui `Paragraph` inside a `Block` with borders and title "Help - Keybindings". Content should be a multi-line `Text` with these sections:

   **Global**
   ```
   1-5       Switch screen (Dashboard/Diagnostics/Keys/PIN/SSH)
   r         Refresh YubiKey status and diagnostics
   ?         Toggle this help screen
   q / Esc   Quit (from Dashboard) or go back
   ```

   **Key Management (Screen 3)**
   ```
   v         View full card status
   i         Import existing key to card
   g         Generate new key on card
   e         Export SSH public key
   Up/Down   Select key (in import view)
   Enter     Execute selected operation
   ```

   **PIN Management (Screen 4)**
   ```
   c         Change user PIN
   a         Change admin PIN
   r         Set reset code
   u         Unblock user PIN
   Enter     Execute selected operation
   ```

   **SSH Wizard (Screen 5)**
   ```
   1-5       Select wizard step
   r         Refresh SSH status
   Enter     Execute selected step
   ```

   Use `Color::Cyan` for section headers, `Color::Yellow` for key names, `Color::White` for descriptions. Style each line as a `Line` with two `Span`s: key (fixed width, yellow) and description (white).

2. In `src/ui/mod.rs`, add `pub mod help;` after the existing module declarations.

3. In `src/ui/mod.rs` `render_status_bar`, add `Screen::Help => "Help"` to the `status_text` match and `Screen::Help => "?: Close Help | ESC: Close Help"` to the `help_text` match.

4. In `src/app.rs`:
   a. Add `Help` to the `Screen` enum.
   b. Add a `previous_screen: Screen` field to `App`, initialized to `Screen::Dashboard` in `App::new`.
   c. In the `render` method, add `Screen::Help => ui::help::render(frame, chunks[0])` to the match.
   d. In `handle_key_event`, in the "Regular navigation" section (after all screen-specific blocks), add a `KeyCode::Char('?')` arm that:
      - Stores `self.current_screen` in `self.previous_screen`
      - Sets `self.current_screen = Screen::Help`
   e. Add a new block at the TOP of `handle_key_event` (before the Keys/SSH/PIN blocks) that checks if `self.current_screen == Screen::Help`. If so, on `Esc` or `?`, restore `self.current_screen = self.previous_screen` and return. This ensures `?` works as a toggle.
   f. Also handle `?` within the screen-specific blocks (Keys, SSH, PIN) by adding it before their `return Ok(())` — specifically, in each screen-specific block's catch-all `_ => {}`, check for `KeyCode::Char('?')` and navigate to Help. Alternatively, move the `?` check to the very top of `handle_key_event` before any screen-specific handling so it works globally from any screen.

5. Run `cargo fmt` and `cargo clippy -- -D warnings`.
  </action>
  <verify>
    <automated>cd C:/Users/mbeha/code/yubitui && cargo clippy -- -D warnings 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/ui/help.rs` succeeds (file exists)
    - `grep -n "pub fn render" src/ui/help.rs` returns at least 1 match
    - `grep -n "pub mod help" src/ui/mod.rs` returns 1 match
    - `grep -n "Screen::Help" src/app.rs` returns at least 4 matches (enum variant, render match, ? handler, Esc handler)
    - `grep -n "previous_screen" src/app.rs` returns at least 3 matches (field, store, restore)
    - `grep "Char('?')" src/app.rs` returns at least 1 match
    - `cargo clippy -- -D warnings` exits 0
    - `cargo fmt -- --check` exits 0
  </acceptance_criteria>
  <done>Pressing ? from any screen opens a help overlay showing all keybindings. Pressing ? or Esc returns to the previous screen. Status bar shows "Help".</done>
</task>

</tasks>

<verification>
- `cargo check` passes
- `cargo clippy -- -D warnings` passes
- `cargo fmt -- --check` passes
- `grep "Screen::Help" src/app.rs` shows the variant exists
- `grep "pub mod help" src/ui/mod.rs` shows module is declared
- `grep "previous_screen" src/app.rs` shows screen-return logic exists
</verification>

<success_criteria>
The `?` key opens a help screen from any screen in the application. The help screen displays a complete keybinding reference organized by screen. Pressing `?` or `Esc` returns to wherever the user was before.
</success_criteria>

<output>
After completion, create `.planning/phases/01-polish-cross-platform-fixes/01-02-SUMMARY.md`
</output>
