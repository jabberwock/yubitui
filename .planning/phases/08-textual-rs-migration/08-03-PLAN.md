---
phase: 08-textual-rs-migration
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/tui/diagnostics.rs
  - src/tui/piv.rs
  - src/tui/ssh.rs
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Diagnostics screen renders as a textual-rs Widget with Header, diagnostic items, and Footer"
    - "PIV screen renders as a textual-rs Widget with sidebar slot list and main detail area"
    - "SSH screen renders as a textual-rs Widget with sidebar summary and main action area"
    - "All three screens have visible keybindings in Footer"
    - "All three screens respond to Esc to navigate back"
  artifacts:
    - path: "src/tui/diagnostics.rs"
      provides: "Diagnostics screen as textual-rs Widget"
      contains: "impl Widget for"
    - path: "src/tui/piv.rs"
      provides: "PIV screen as textual-rs Widget"
      contains: "impl Widget for"
    - path: "src/tui/ssh.rs"
      provides: "SSH screen as textual-rs Widget"
      contains: "impl Widget for"
  key_links:
    - from: "src/tui/diagnostics.rs"
      to: "crate::diagnostics::Diagnostics"
      via: "reads diagnostics data for display"
      pattern: "Diagnostics"
    - from: "src/tui/ssh.rs"
      to: "crate::model::ssh"
      via: "reads SSH state for display"
      pattern: "SshState"
---

<objective>
Migrate Diagnostics (175 lines), PIV (144 lines), and SSH (480 lines) screens to textual-rs Widgets.

Purpose: These three small-to-medium screens are independent and establish the migration pattern for more complex screens. After this plan, 4 of 7 screens are migrated.
Output: Three fully migrated textual-rs Widget screens with Header, Footer, keybindings, and proper rule-of-thirds layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-textual-rs-migration/08-CONTEXT.md
@.planning/phases/08-textual-rs-migration/08-RESEARCH.md
@.planning/phases/08-textual-rs-migration/08-UI-SPEC.md
@.planning/phases/08-textual-rs-migration/08-02-SUMMARY.md

@src/tui/diagnostics.rs
@src/tui/piv.rs
@src/tui/ssh.rs
@src/tui/help.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Diagnostics screen to textual-rs Widget</name>
  <files>src/tui/diagnostics.rs</files>
  <read_first>
    - src/tui/diagnostics.rs (full 175-line implementation being migrated)
    - src/tui/help.rs (reference — the already-migrated Widget pattern from Plan 02)
    - .planning/phases/08-textual-rs-migration/08-UI-SPEC.md (Diagnostics keybindings, layout contract)
    - src/diagnostics.rs (the Diagnostics data struct — NOT being modified, just read for field names)
  </read_first>
  <action>
Migrate Diagnostics screen following the same Widget pattern established in Help (Plan 02).

Per D-04: Keep `DiagnosticsTuiState` and `DiagnosticsAction` enums. Per D-07/D-15: Footer with keybindings. Per D-13: Rule-of-thirds layout with sidebar (category list) and main (diagnostic detail/result) per UI-SPEC.

1. **Rewrite** `src/tui/diagnostics.rs`:

Keep existing types:
- `DiagnosticsTuiState` (scroll_offset field)
- `DiagnosticsAction` enum (None, NavigateTo)

Create `DiagnosticsScreen` struct implementing `Widget`:
- Fields: `diagnostics: Diagnostics` (the data), `state: Reactive<DiagnosticsTuiState>`
- `compose()`: Header("Diagnostics") + sidebar (category list: PC/SC, GPG, SSH, Card) + main (selected category detail) + Footer
- `key_bindings()`: Esc=back, r=run_diagnostics (per UI-SPEC keybinding table)
- `on_action()`: "back" -> pop_screen_deferred, "run_diagnostics" -> re-run diagnostics

2. Port all diagnostic rendering from the current `render()` function:
   - PC/SC Daemon status (pcscd.running check)
   - GPG agent status
   - SSH agent status
   - Card reader detection
   - Use textual-rs Static/content widgets instead of ratatui ListItem
   - Use TCSS theme variables ($success, $warning, $error) for status colors instead of hardcoded Color::Green/Yellow/Red

3. Layout per UI-SPEC:
   - Sidebar (33%): Category list
   - Main (67%): Selected category diagnostic output
   - If no sidebar is natural for diagnostics (all items in one list), use full-width like Help

4. Delete the old `render()` free function and `handle_key()` free function.
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/diagnostics.rs contains `impl Widget for DiagnosticsScreen`
    - src/tui/diagnostics.rs contains `fn key_bindings(`
    - src/tui/diagnostics.rs contains `Footer`
    - src/tui/diagnostics.rs contains `Header::new(`
    - src/tui/diagnostics.rs does NOT contain `fn render(frame: &mut Frame`
    - src/tui/diagnostics.rs does NOT contain `Color::Cyan` or `Color::Green` (no hardcoded colors)
    - `cargo check` exits 0
  </acceptance_criteria>
  <done>Diagnostics screen is a textual-rs Widget with Header, Footer, keybindings, themed status colors</done>
</task>

<task type="auto">
  <name>Task 2: Migrate PIV and SSH screens to textual-rs Widgets</name>
  <files>
    src/tui/piv.rs
    src/tui/ssh.rs
  </files>
  <read_first>
    - src/tui/piv.rs (full 144-line implementation)
    - src/tui/ssh.rs (full 480-line implementation)
    - src/tui/help.rs (reference Widget pattern from Plan 02)
    - .planning/phases/08-textual-rs-migration/08-UI-SPEC.md (PIV and SSH keybindings, layout, copywriting)
    - src/model/piv.rs (PivState data — read for field names, NOT modified)
    - src/model/ssh.rs (SshState data — read for field names, NOT modified)
  </read_first>
  <action>
Migrate PIV (144 lines) and SSH (480 lines) screens following the Widget pattern.

**PIV Screen:**

Per D-04: Keep `PivTuiState` and `PivAction` enums. Per UI-SPEC: sidebar = slot list (4 PIV slots), main = slot detail/certificate info.

1. Create `PivScreen` struct implementing `Widget`:
   - Fields: `app_state` reference (for PIV slot data), `state: Reactive<PivTuiState>`
   - `compose()`: Header("PIV Certificates") + sidebar (slot list: Authentication, Digital Signature, Key Management, Card Authentication) + main (selected slot detail) + Footer
   - `key_bindings()`: Esc=back, v=view_slot, r=refresh (per UI-SPEC)
   - `on_action()`: dispatch to existing PivAction logic
   - Slot list items should be Button widgets (per D-06)
   - Primary CTA: "View Slot" button (per UI-SPEC copywriting)

2. Delete old `render()` and `handle_key()` free functions.

**SSH Screen:**

Per D-04: Keep `SshState` and `SshAction` enums. Per UI-SPEC: sidebar = agent status summary, main = SSH key details / action buttons.

1. Create `SshScreen` struct implementing `Widget`:
   - Fields: `ssh_state: Reactive<SshState>`, `app_state` reference
   - `compose()`: Header("SSH Setup Wizard") + sidebar (agent status summary) + main (key details, action buttons) + Footer
   - `key_bindings()`: Esc=back, a=add_to_agent, r=refresh (per UI-SPEC)
   - `on_action()`: dispatch to existing SshAction logic
   - "Add to Agent" as a Button widget (per D-06, UI-SPEC primary CTA)

2. Port all SSH rendering logic:
   - SSH key fingerprint display
   - Agent status (running/not running)
   - Auth key slot status
   - SshScreen enum variants (Main, Enable) — these can become internal reactive state or pushed sub-screens
   - Use theme variables instead of hardcoded colors

3. Delete old `render()` free function.
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/piv.rs contains `impl Widget for PivScreen`
    - src/tui/piv.rs contains `fn key_bindings(` and `Footer`
    - src/tui/piv.rs does NOT contain `fn render(frame: &mut Frame`
    - src/tui/ssh.rs contains `impl Widget for SshScreen`
    - src/tui/ssh.rs contains `fn key_bindings(` and `Footer`
    - src/tui/ssh.rs does NOT contain `fn render(frame: &mut Frame`
    - Neither file contains hardcoded `Color::` values
    - `cargo check` exits 0
  </acceptance_criteria>
  <done>PIV and SSH screens are textual-rs Widgets with Header, sidebar+main layout, Footer with keybindings, themed colors</done>
</task>

</tasks>

<verification>
- `cargo check` exits 0
- All three screens (Diagnostics, PIV, SSH) implement Widget trait
- All three screens have Footer with visible keybindings
- No hardcoded `Color::` values in any of the three files
- No old `render(frame: &mut Frame, ...)` functions remain in these files
</verification>

<success_criteria>
Diagnostics, PIV, and SSH screens are all textual-rs Widgets. After this plan, 4 of 7 screens are migrated (Help + these three). All use Header/Footer/keybindings pattern consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/08-textual-rs-migration/08-03-SUMMARY.md`
</output>
