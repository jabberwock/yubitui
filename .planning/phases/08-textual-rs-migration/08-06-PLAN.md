---
phase: 08-textual-rs-migration
plan: 06
type: execute
wave: 5
depends_on: ["08-05"]
files_modified:
  - src/tui/dashboard.rs
  - src/tui/keys.rs
  - src/tui/pin.rs
  - src/tui/piv.rs
  - src/tui/ssh.rs
  - src/tui/diagnostics.rs
  - src/tui/help.rs
  - src/tui/snapshots/yubitui__tui__dashboard__tests__dashboard_default_populated.snap
  - src/tui/snapshots/yubitui__tui__dashboard__tests__dashboard_no_yubikey.snap
  - src/tui/snapshots/yubitui__tui__dashboard__tests__dashboard_context_menu_open.snap
  - src/tui/snapshots/yubitui__tui__diagnostics__tests__diagnostics_default.snap
  - src/tui/snapshots/yubitui__tui__help__tests__help_screen.snap
  - src/tui/snapshots/yubitui__tui__keys__tests__keys_default_state.snap
  - src/tui/snapshots/yubitui__tui__keys__tests__keys_import_screen.snap
  - src/tui/snapshots/yubitui__tui__keys__tests__keys_no_yubikey.snap
  - src/tui/snapshots/yubitui__tui__pin__tests__pin_default_state.snap
  - src/tui/snapshots/yubitui__tui__pin__tests__pin_no_yubikey.snap
  - src/tui/snapshots/yubitui__tui__pin__tests__pin_unblock_wizard.snap
  - src/tui/snapshots/yubitui__tui__piv__tests__piv_default_state.snap
  - src/tui/snapshots/yubitui__tui__piv__tests__piv_no_yubikey.snap
  - src/tui/snapshots/yubitui__tui__ssh__tests__ssh_enable_screen.snap
  - src/tui/snapshots/yubitui__tui__ssh__tests__ssh_main_screen.snap
autonomous: false
requirements:
  - INFRA-03

must_haves:
  truths:
    - "All 15 insta snapshots are updated to reflect new textual-rs layout"
    - "Every screen has at least one Pilot smoke test (TestApp + pilot.settle + snapshot)"
    - "Every screen has at least one Pilot navigation test (pilot.press + buffer assertion)"
    - "cargo test passes with zero failures"
    - "No tmux E2E tests exist — all coverage is Pilot-based"
  artifacts:
    - path: "src/tui/dashboard.rs"
      provides: "Pilot tests for Dashboard"
      contains: "TestApp::new"
    - path: "src/tui/help.rs"
      provides: "Pilot tests for Help"
      contains: "TestApp::new"
    - path: "src/tui/keys.rs"
      provides: "Pilot tests for Keys"
      contains: "TestApp::new"
    - path: "src/tui/pin.rs"
      provides: "Pilot tests for Pin"
      contains: "TestApp::new"
  key_links:
    - from: "all screen test modules"
      to: "textual_rs::testing::TestApp"
      via: "TestApp::new(80, 24, factory) + pilot()"
      pattern: "TestApp::new"
    - from: "all screen test modules"
      to: "insta::assert_display_snapshot!"
      via: "snapshot assertion on TestApp buffer"
      pattern: "assert_display_snapshot"
---

<objective>
Write Pilot-based tests for all 7 screens and re-accept insta snapshots for the new textual-rs layout.

Purpose: This replaces all tmux E2E coverage (D-08/D-09) with textual-rs Pilot tests that run in cargo test. Snapshot re-acceptance accounts for the layout change from rule-of-thirds migration (RESEARCH.md Pitfall 1).
Output: All 7 screens have Pilot smoke + navigation tests. All 15 snapshots updated. cargo test passes fully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-textual-rs-migration/08-CONTEXT.md
@.planning/phases/08-textual-rs-migration/08-RESEARCH.md
@.planning/phases/08-textual-rs-migration/08-UI-SPEC.md
@.planning/phases/08-textual-rs-migration/08-05-SUMMARY.md

@src/tui/dashboard.rs
@src/tui/help.rs
@src/tui/diagnostics.rs
@src/tui/keys.rs
@src/tui/pin.rs
@src/tui/piv.rs
@src/tui/ssh.rs
@src/model/mock.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Pilot-based tests for all 7 screens</name>
  <files>
    src/tui/dashboard.rs
    src/tui/keys.rs
    src/tui/pin.rs
    src/tui/piv.rs
    src/tui/ssh.rs
    src/tui/diagnostics.rs
    src/tui/help.rs
  </files>
  <read_first>
    - src/tui/dashboard.rs (current state after Plan 05 migration)
    - src/tui/help.rs (current state)
    - src/model/mock.rs (mock_yubikey_states() function for test fixtures)
    - .planning/phases/08-textual-rs-migration/08-RESEARCH.md (Pattern 4: insta compatibility, Pattern 5: Pilot tests)
    - .planning/phases/08-textual-rs-migration/08-UI-SPEC.md (Test Contract section)
  </read_first>
  <action>
Per D-09: All screen coverage replaced by Pilot-based tests in cargo test. Per D-10: insta snapshots kept.
Per UI-SPEC Test Contract: every screen needs (1) one Pilot smoke test, (2) one Pilot navigation test.

Add `#[cfg(test)] mod tests { ... }` block to each screen file (or update existing test modules) with these tests:

**Pattern for each screen (verified from RESEARCH.md Pattern 4 + 5):**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use textual_rs::testing::TestApp;
    use crossterm::event::KeyCode;

    #[tokio::test]
    async fn screen_name_smoke() {
        let mock_state = crate::model::mock::mock_yubikey_states();
        let mut app = TestApp::new(80, 24, || {
            Box::new(ScreenName::new(/* appropriate args from mock_state */))
        });
        app.pilot().settle().await;
        insta::assert_display_snapshot!(app.backend());
    }

    #[tokio::test]
    async fn screen_name_navigation() {
        let mut app = TestApp::new(80, 24, || {
            Box::new(ScreenName::new(/* args */))
        });
        let mut pilot = app.pilot();
        pilot.press(KeyCode::Esc).await;
        pilot.settle().await;
        // Assert expected behavior (screen popped, or specific content visible)
    }
}
```

**Per-screen test specifics:**

1. **Dashboard** (3 tests):
   - `dashboard_default_populated` — mock YubiKey state, settle, snapshot
   - `dashboard_no_yubikey` — empty state, settle, snapshot
   - `dashboard_context_menu_open` — press 'm', settle, snapshot (or assert menu content visible)

2. **Keys** (3 tests):
   - `keys_default_state` — mock state with keys, settle, snapshot
   - `keys_no_yubikey` — empty state, settle, snapshot
   - `keys_import_screen` — press 'i', settle, assert import screen content visible

3. **PIN** (3 tests):
   - `pin_default_state` — mock state, settle, snapshot
   - `pin_no_yubikey` — empty state, settle, snapshot
   - `pin_unblock_wizard` — press 'u', settle, assert unblock screen content visible

4. **PIV** (2 tests):
   - `piv_default_state` — mock state, settle, snapshot
   - `piv_no_yubikey` — empty state, settle, snapshot

5. **SSH** (2 tests):
   - `ssh_main_screen` — mock state, settle, snapshot
   - `ssh_enable_screen` — press 'a', settle, assert enable content visible

6. **Diagnostics** (1 test):
   - `diagnostics_default` — default diagnostics data, settle, snapshot

7. **Help** (1 test):
   - `help_screen` — settle, snapshot

Total: 15 Pilot tests covering all 7 screens (matching the 15 existing snapshot files).

The exact TestApp/Pilot API must be confirmed from the textual-rs crate at implementation time. The executor should `cargo doc --open -p textual-rs` to verify TestApp::new signature, pilot() method, settle/press semantics.
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo test 2>&1 | grep -E "test result|FAILED" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every screen file contains `#[tokio::test]` with `TestApp::new(`
    - `grep -r "TestApp::new" src/tui/` returns matches in all 7 screen files
    - `grep -r "pilot" src/tui/` returns matches in all 7 screen files
    - `grep -r "assert_display_snapshot" src/tui/` returns matches in all 7 screen files
    - `cargo test` compiles (may fail on snapshot mismatches initially — Task 2 handles re-acceptance)
  </acceptance_criteria>
  <done>All 7 screens have Pilot-based smoke and navigation tests using TestApp and insta snapshots</done>
</task>

<task type="auto">
  <name>Task 2: Re-accept insta snapshots and verify full test suite</name>
  <files>
    src/tui/snapshots/yubitui__tui__dashboard__tests__dashboard_default_populated.snap
    src/tui/snapshots/yubitui__tui__dashboard__tests__dashboard_no_yubikey.snap
    src/tui/snapshots/yubitui__tui__dashboard__tests__dashboard_context_menu_open.snap
    src/tui/snapshots/yubitui__tui__diagnostics__tests__diagnostics_default.snap
    src/tui/snapshots/yubitui__tui__help__tests__help_screen.snap
    src/tui/snapshots/yubitui__tui__keys__tests__keys_default_state.snap
    src/tui/snapshots/yubitui__tui__keys__tests__keys_import_screen.snap
    src/tui/snapshots/yubitui__tui__keys__tests__keys_no_yubikey.snap
    src/tui/snapshots/yubitui__tui__pin__tests__pin_default_state.snap
    src/tui/snapshots/yubitui__tui__pin__tests__pin_no_yubikey.snap
    src/tui/snapshots/yubitui__tui__pin__tests__pin_unblock_wizard.snap
    src/tui/snapshots/yubitui__tui__piv__tests__piv_default_state.snap
    src/tui/snapshots/yubitui__tui__piv__tests__piv_no_yubikey.snap
    src/tui/snapshots/yubitui__tui__ssh__tests__ssh_enable_screen.snap
    src/tui/snapshots/yubitui__tui__ssh__tests__ssh_main_screen.snap
  </files>
  <read_first>
    - src/tui/snapshots/ (list all .snap files to understand current state)
    - .planning/phases/08-textual-rs-migration/08-RESEARCH.md (Pitfall 1: Snapshot Invalidation on Migration)
  </read_first>
  <action>
Per RESEARCH.md Pitfall 1: Layout changes from rule-of-thirds migration invalidate all existing snapshots. They must be re-accepted.

1. Run tests with snapshot update mode:
   ```bash
   INSTA_UPDATE=always cargo test
   ```
   This will auto-accept all new snapshot content.

2. Review the updated snapshots manually — verify they show:
   - Header at top of each screen
   - Footer with keybindings at bottom of each screen
   - Rule-of-thirds layout where applicable (sidebar + main content)
   - No hardcoded color codes (theme variables resolve to theme-specific colors)
   - Button widgets visible with borders

3. If any snapshots show rendering issues (empty content, broken layout), fix the corresponding screen Widget and re-run.

4. Run the full test suite to confirm zero failures:
   ```bash
   cargo test
   ```

5. Verify snapshot count matches expectations:
   ```bash
   ls src/tui/snapshots/*.snap | wc -l
   ```
   Should be >= 15 snapshot files (may be more if new test names differ from old ones).

6. Clean up any orphaned snapshots from old test names:
   ```bash
   cargo insta test --delete-unreferenced-snapshots
   ```
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo test 2>&1 | grep "test result" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits 0 with "test result: ok" and zero failures
    - `ls src/tui/snapshots/*.snap | wc -l` returns >= 15
    - No `.snap.new` files exist (all snapshots accepted)
    - `find src/tui/snapshots/ -name "*.snap.new" | wc -l` returns 0
  </acceptance_criteria>
  <done>All insta snapshots re-accepted for new textual-rs layout, full test suite passes with zero failures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete textual-rs migration</name>
  <files>src/app.rs</files>
  <action>
Present the completed migration to the user for visual and functional verification. The app should demonstrate all phase 8 success criteria:
- All 7 screens render via textual-rs components
- src/model/ is byte-for-byte unchanged
- tmux E2E harness retired, Pilot tests in its place
- Theme selection works
- All mouse click navigation and keyboard shortcuts work identically
- CI passes (cargo test)
  </action>
  <verify>User confirms the migration is visually and functionally correct</verify>
  <done>User types "approved" confirming the migration is complete</done>
  <what-built>
Complete textual-rs migration of all 7 screens. The app now uses:
- textual-rs App runner (no crossterm event loop)
- Header/Footer on every screen with visible keybindings
- Rule-of-thirds layout (sidebar + main content)
- Button widgets for all click targets
- Pushed screens for wizards and dialogs
- Theme support with tokyo-night default
- Pilot-based tests replacing tmux E2E
  </what-built>
  <how-to-verify>
1. Run `cargo run -- --mock` and verify:
   - Dashboard shows with device info sidebar and 6 navigation buttons
   - Press 1-6 to navigate to each screen — all render with Header + Footer
   - Footer shows keybindings on every screen
   - Esc navigates back to Dashboard from any screen
   - Ctrl+T cycles through themes (tokyo-night -> nord -> gruvbox -> dracula -> catppuccin)
2. On Keys screen: press G to start key generation wizard — verify multi-step flow works
3. On PIN screen: press C to start Change PIN — verify PIN input wizard works
4. Verify mouse: click any Button widget — it should activate
5. Run `cargo test` — should show all tests passing
6. Verify no old infrastructure remains:
   - `ls tests/e2e/` should fail (directory deleted)
   - `grep -r "ClickRegion" src/` should return nothing
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the migration</resume-signal>
</task>

</tasks>

<verification>
- `cargo test` exits 0 — all Pilot tests and snapshot tests pass
- All 7 screens have at least one Pilot smoke test and one navigation test
- No `.snap.new` files remain (all snapshots accepted)
- No tmux E2E tests exist
- User has verified the visual migration looks correct
</verification>

<success_criteria>
Phase 8 is complete: all 7 screens are textual-rs Widgets with Header/Footer/keybindings, all tests pass via Pilot + insta, tmux harness retired, ClickRegion retired, theme support working. src/model/ is byte-for-byte unchanged (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/08-textual-rs-migration/08-06-SUMMARY.md`
</output>
