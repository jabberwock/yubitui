---
phase: 08-textual-rs-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/model/click_region.rs
  - src/model/app_state.rs
  - src/model/mod.rs
  - src/tui/mod.rs
  - src/tui/dashboard.rs
  - src/tui/keys.rs
  - src/tui/pin.rs
  - src/tui/piv.rs
  - src/tui/ssh.rs
  - src/tui/diagnostics.rs
  - src/tui/help.rs
  - src/app.rs
  - tests/e2e/dashboard_smoke.sh
  - tests/e2e/diagnostics_smoke.sh
  - tests/e2e/helpers.sh
  - tests/e2e/keys_smoke.sh
  - tests/e2e/pin_smoke.sh
  - tests/e2e/piv_smoke.sh
  - tests/e2e/run_all.sh
  - tests/e2e/ssh_smoke.sh
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "cargo check compiles cleanly with ratatui 0.30, crossterm 0.29, and textual-rs 0.2"
    - "All existing tests pass (cargo test) after dependency upgrade and ClickRegion removal"
    - "tmux E2E harness is completely deleted"
    - "No ClickRegion, ClickAction, or click_regions references remain in the codebase"
  artifacts:
    - path: "Cargo.toml"
      provides: "Updated dependency versions"
      contains: "textual-rs"
    - path: "src/model/app_state.rs"
      provides: "AppState without click_regions field"
  key_links:
    - from: "Cargo.toml"
      to: "all src/*.rs"
      via: "ratatui 0.30 + crossterm 0.29 API compatibility"
      pattern: "ratatui.*0\\.30"
---

<objective>
Upgrade dependencies to ratatui 0.30 + crossterm 0.29 + add textual-rs 0.2, fix all ratatui 0.30 breaking changes, delete the ClickRegion infrastructure, and retire the tmux E2E harness.

Purpose: This is the structural foundation — every subsequent plan depends on textual-rs being available and the old click region / tmux infrastructure being gone.
Output: A compiling codebase with textual-rs available, all old infrastructure removed, existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-textual-rs-migration/08-CONTEXT.md
@.planning/phases/08-textual-rs-migration/08-RESEARCH.md

@Cargo.toml
@src/model/click_region.rs
@src/model/app_state.rs
@src/model/mod.rs
@src/tui/mod.rs
@src/app.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Cargo.toml dependencies and MSRV</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current dependency versions)
    - .planning/phases/08-textual-rs-migration/08-RESEARCH.md (exact version strings and feature flags)
  </read_first>
  <action>
Update Cargo.toml with these exact changes:

1. Replace the TUI Framework section:
   - Remove: `ratatui = "0.29"`
   - Remove: `crossterm = { version = "0.28", features = ["event-stream"] }`
   - Add: `ratatui = { version = "0.30.0", features = ["crossterm_0_29"] }`
   - Add: `crossterm = { version = "0.29.0", features = ["event-stream"] }`
   - Add: `textual-rs = "0.2"`

2. Update MSRV:
   - Change: `rust-version = "1.75"` to `rust-version = "1.88"`

3. Add tokio to dev-dependencies if not already present (textual-rs Pilot tests require tokio runtime):
   - Under `[dev-dependencies]` add: `tokio = { version = "1.40", features = ["macros", "rt-multi-thread"] }` (only if not already there as a dev-dep; it IS already in `[dependencies]`)

Per D-01: textual-rs replaces raw ratatui composition but sits on top of ratatui. This is an addition, not a removal.
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `ratatui = { version = "0.30.0", features = ["crossterm_0_29"] }`
    - Cargo.toml contains `crossterm = { version = "0.29.0", features = ["event-stream"] }`
    - Cargo.toml contains `textual-rs = "0.2"`
    - Cargo.toml contains `rust-version = "1.88"`
    - `cargo check` exits 0 (may have warnings, no errors)
  </acceptance_criteria>
  <done>Cargo.toml updated with all three dependency upgrades and MSRV bump, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Delete ClickRegion infrastructure and strip click_regions from all screens</name>
  <files>
    src/model/click_region.rs
    src/model/app_state.rs
    src/model/mod.rs
    src/tui/mod.rs
    src/tui/dashboard.rs
    src/tui/keys.rs
    src/tui/pin.rs
    src/tui/piv.rs
    src/tui/ssh.rs
    src/tui/diagnostics.rs
    src/tui/help.rs
    src/app.rs
  </files>
  <read_first>
    - src/model/click_region.rs (full file — being deleted)
    - src/model/app_state.rs (has click_regions field and ClickRegion import)
    - src/model/mod.rs (has pub mod click_region)
    - src/tui/mod.rs (has From&lt;Rect&gt; for Region impl)
    - src/app.rs (has click region dispatch in handle_mouse_event, apply_mouse_capture, render calls passing &amp;mut click_regions)
    - src/tui/dashboard.rs (has click_regions parameter in render, push calls)
    - src/tui/keys.rs (has click_regions parameter in render, push calls)
    - src/tui/pin.rs (has click_regions parameter in render, push calls)
    - src/tui/piv.rs (has click_regions parameter in render, push calls)
    - src/tui/ssh.rs (has click_regions parameter in render, push calls)
    - src/tui/diagnostics.rs (has click_regions parameter in render, push calls)
    - src/tui/help.rs (has click_regions parameter in render, push calls)
  </read_first>
  <action>
Per D-05: ClickRegionMap/ClickRegion infrastructure is retired. textual-rs Button widgets replace it.

1. **Delete** `src/model/click_region.rs` entirely.

2. **Update** `src/model/mod.rs`:
   - Remove the line `pub mod click_region;`

3. **Update** `src/model/app_state.rs`:
   - Remove `use super::click_region::ClickRegion;`
   - Remove the `click_regions: Vec<ClickRegion>` field from `AppState` struct
   - Remove `#[serde(skip)]` annotation that was on click_regions
   - Remove `click_regions: Vec::new()` from the `Default` impl

4. **Update** `src/tui/mod.rs`:
   - Remove the entire `impl From<ratatui::layout::Rect> for crate::model::click_region::Region` block (lines 10-18)

5. **Update all 7 screen render functions** to remove click_regions parameter:
   For each of: dashboard.rs, keys.rs, pin.rs, piv.rs, ssh.rs, diagnostics.rs, help.rs:
   - Remove `click_regions: &mut Vec<crate::model::click_region::ClickRegion>` from the render function signature
   - Remove all `click_regions.clear()` calls
   - Remove all `click_regions.push(ClickRegion { ... })` calls and the surrounding Region/ClickAction construction code
   - Remove any `use crate::model::click_region::*` imports
   - Keep ALL other rendering logic intact — only strip click region registration

6. **Update** `src/app.rs`:
   - Remove the `handle_mouse_click` method or any method that dispatches ClickAction
   - Remove `use crate::model::click_region::*` imports
   - Update all render() call sites to no longer pass `&mut self.state.click_regions`
   - Remove any `self.state.click_regions.clear()` calls
   - Keep the mouse event loop code for now (it will be replaced in Plan 02 when app.rs is rewritten) — just remove the click_region dispatch parts
   - The `mouse_capture_enabled` field and `apply_mouse_capture()` can stay for now

Per D-03: src/model/ changes are limited to removing click_region.rs and the click_regions field from AppState. These are TUI-infrastructure types, not model data. The model data (Screen, YubiKeyState, etc.) is untouched.
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/model/click_region.rs does not exist
    - `grep -r "ClickRegion\|click_regions\|ClickAction" src/` returns no matches
    - `grep -r "click_region" src/model/mod.rs` returns no matches
    - `cargo test` exits 0
    - src/model/app_state.rs does NOT contain "click_regions" field
    - src/tui/mod.rs does NOT contain "From<ratatui::layout::Rect>"
  </acceptance_criteria>
  <done>ClickRegion infrastructure completely removed from the codebase, all render functions simplified, cargo test passes</done>
</task>

<task type="auto">
  <name>Task 3: Delete tmux E2E test harness</name>
  <files>
    tests/e2e/dashboard_smoke.sh
    tests/e2e/diagnostics_smoke.sh
    tests/e2e/helpers.sh
    tests/e2e/keys_smoke.sh
    tests/e2e/pin_smoke.sh
    tests/e2e/piv_smoke.sh
    tests/e2e/run_all.sh
    tests/e2e/ssh_smoke.sh
  </files>
  <read_first>
    - tests/e2e/run_all.sh (understand full scope of what's being deleted)
  </read_first>
  <action>
Per D-08: tmux E2E harness is retired. All screen coverage will be replaced by textual-rs Pilot-based tests in Plan 06.

1. Delete the entire `tests/e2e/` directory and all files within it:
   - tests/e2e/dashboard_smoke.sh
   - tests/e2e/diagnostics_smoke.sh
   - tests/e2e/helpers.sh
   - tests/e2e/keys_smoke.sh
   - tests/e2e/pin_smoke.sh
   - tests/e2e/piv_smoke.sh
   - tests/e2e/run_all.sh
   - tests/e2e/ssh_smoke.sh

2. Remove the `tests/e2e/` directory itself.

3. Check if any CI config (`.github/workflows/` or similar) references `tests/e2e/run_all.sh` or tmux — if so, remove those CI steps. (Check with: `grep -r "e2e\|tmux\|run_all" .github/ 2>/dev/null`)
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && test ! -d tests/e2e && echo "PASS: tests/e2e deleted" || echo "FAIL: tests/e2e still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/` directory does not exist
    - `find . -name "*.sh" -path "*/e2e/*"` returns no results
    - No CI configuration references tests/e2e or run_all.sh
  </acceptance_criteria>
  <done>tmux E2E harness completely deleted, no shell-based test scripts remain in tests/e2e/</done>
</task>

</tasks>

<verification>
- `cargo check` exits 0 with ratatui 0.30, crossterm 0.29, textual-rs 0.2
- `cargo test` exits 0 — all remaining tests pass (insta snapshots still valid since rendering code unchanged)
- `grep -r "ClickRegion\|click_regions\|ClickAction\|click_region" src/` returns zero matches
- `tests/e2e/` directory does not exist
- `src/model/click_region.rs` does not exist
</verification>

<success_criteria>
The codebase compiles with textual-rs 0.2 available, all ClickRegion infrastructure is deleted, tmux E2E harness is deleted, and all cargo tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-textual-rs-migration/08-01-SUMMARY.md`
</output>
