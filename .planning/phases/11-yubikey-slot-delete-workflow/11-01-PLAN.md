---
phase: 11-yubikey-slot-delete-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/model/otp.rs
  - src/model/mod.rs
  - src/model/mock.rs
  - src/model/detection.rs
  - src/model/app_state.rs
  - src/tui/otp.rs
  - src/tui/mod.rs
  - src/tui/dashboard.rs
autonomous: true
requirements: [OTP-01]
must_haves:
  truths:
    - "User can open the OTP screen from dashboard via key 9 and see slot 1 and slot 2 status"
    - "Slot status displays Occupied or Empty (type is not readable from hardware)"
    - "Mock mode shows deterministic OTP fixture data"
  artifacts:
    - path: "src/model/otp.rs"
      provides: "OtpState, OtpSlotStatus types + get_otp_slot_status() APDU function"
      contains: "pub enum OtpSlotStatus"
    - path: "src/tui/otp.rs"
      provides: "OtpScreen widget with slot status display"
      contains: "impl Widget for OtpScreen"
  key_links:
    - from: "src/tui/dashboard.rs"
      to: "src/tui/otp.rs"
      via: "nav_9 action pushes OtpScreen"
      pattern: "nav_9.*OtpScreen"
    - from: "src/model/otp.rs"
      to: "pcsc"
      via: "SELECT OTP AID + READ STATUS APDU"
      pattern: "kill_scdaemon|SELECT_OTP|READ_OTP_STATUS"
---

<objective>
Add OTP slot status model, APDU reader, OTP screen widget, and dashboard navigation.

Purpose: Users can view which OTP slots (1 and 2) are occupied on their YubiKey, completing the last hardware-info screen. The credential type is not readable from hardware (confirmed by research) so status is Occupied/Empty only.
Output: src/model/otp.rs (types + APDU), src/tui/otp.rs (screen widget), updated dashboard with nav_9
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-yubikey-slot-delete-workflow/11-RESEARCH.md
@src/model/piv.rs
@src/model/mod.rs
@src/model/mock.rs
@src/model/app_state.rs
@src/model/detection.rs
@src/tui/dashboard.rs
@src/tui/piv.rs
@src/tui/mod.rs

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/model/mod.rs:
```rust
pub struct YubiKeyState {
    pub info: YubiKeyInfo,
    pub openpgp: Option<openpgp::OpenPgpState>,
    pub oath: Option<oath::OathState>,
    pub piv: Option<piv::PivState>,
    pub fido2: Option<fido2::Fido2State>,
    pub pin_status: pin::PinStatus,
    pub touch_policies: Option<touch_policy::TouchPolicies>,
}
```

From src/model/app_state.rs:
```rust
pub enum Screen {
    Dashboard, Diagnostics, Fido2, Help, Keys, Oath, PinManagement, SshWizard, Piv,
}
```

From src/model/piv.rs (pattern to follow for OTP APDU):
```rust
pub fn get_piv_state() -> Result<PivState> {
    super::card::kill_scdaemon();
    std::thread::sleep(std::time::Duration::from_millis(50));
    // ... PC/SC connect, SELECT AID, read status
}
```

From src/tui/dashboard.rs:
```rust
// nav_8 pattern (last added):
"nav_8" => {
    let fido2_state = self.app_state.yubikey_state()
        .and_then(|yk| yk.fido2.clone());
    ctx.push_screen_deferred(Box::new(crate::tui::fido2::Fido2Screen::new(fido2_state)));
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OTP model types, APDU function, mock fixture, and Screen enum</name>
  <files>src/model/otp.rs, src/model/mod.rs, src/model/mock.rs, src/model/detection.rs, src/model/app_state.rs</files>
  <read_first>
    - src/model/piv.rs (pattern for APDU function — kill_scdaemon, connect exclusive, transmit)
    - src/model/mod.rs (YubiKeyState struct — add otp field)
    - src/model/mock.rs (mock fixture — add otp field)
    - src/model/detection.rs (detect_all_yubikey_states — wire get_otp_slot_status call)
    - src/model/app_state.rs (Screen enum — add Otp variant)
    - src/model/card.rs (kill_scdaemon, apdu_sw helper functions)
  </read_first>
  <action>
    1. Create `src/model/otp.rs` with:
       - `OtpSlotStatus` enum: `Occupied`, `Empty` (derive Debug, Clone, serde::Serialize)
       - `OtpState` struct with fields: `slot1: OtpSlotStatus`, `slot2: OtpSlotStatus`, `slot1_touch: bool`, `slot2_touch: bool` (derive Debug, Clone, serde::Serialize)
       - Constants: `OTP_AID: &[u8] = &[0xA0, 0x00, 0x00, 0x05, 0x27, 0x20, 0x01, 0x01]`
       - Constants: `SELECT_OTP: &[u8] = &[0x00, 0xA4, 0x04, 0x00, 0x08, 0xA0, 0x00, 0x00, 0x05, 0x27, 0x20, 0x01, 0x01]`
       - Constants: `READ_OTP_STATUS: &[u8] = &[0x00, 0x03, 0x00, 0x00]`
       - Bitmask constants: `SLOT1_VALID: u16 = 0x01`, `SLOT2_VALID: u16 = 0x02`, `SLOT1_TOUCH: u16 = 0x04`, `SLOT2_TOUCH: u16 = 0x08`
       - `pub fn get_otp_slot_status() -> Result<OtpState>` following the piv.rs pattern exactly:
         call `super::card::kill_scdaemon()`, sleep 50ms, establish PC/SC context (Scope::User),
         list readers, connect first reader (ShareMode::Exclusive, Protocols::T0 | T1),
         transmit SELECT_OTP (check SW 0x9000), transmit READ_OTP_STATUS (check SW 0x9000 and len >= 6),
         parse touch_level from bytes [4] and [5] as `(resp[5] as u16) << 8 | (resp[4] as u16)`,
         return OtpState with slot status from bitmask
       - Unit test: `test_otp_slot_status_bitmask` that tests the bitmask parsing logic directly (extract the parsing into a helper `fn parse_otp_status(status_bytes: &[u8]) -> Result<OtpState>` so it can be tested without hardware)

    2. In `src/model/mod.rs`:
       - Add `pub mod otp;` line after the existing `pub mod oath;` line
       - Add `pub otp: Option<otp::OtpState>,` field to `YubiKeyState` struct after the `fido2` field

    3. In `src/model/mock.rs`:
       - Add `otp: Some(otp::OtpState { slot1: otp::OtpSlotStatus::Occupied, slot2: otp::OtpSlotStatus::Empty, slot1_touch: false, slot2_touch: false }),` to the mock YubiKeyState after the `fido2` field

    4. In `src/model/detection.rs`:
       - In `detect_all_yubikey_states()`, after the PIV state detection block, add OTP detection:
         `let otp = super::otp::get_otp_slot_status().ok();`
         Add `otp,` to the YubiKeyState construction

    5. In `src/model/app_state.rs`:
       - Add `Otp,` variant to the `Screen` enum after `Oath,`
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo test model::otp -- --nocapture 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/model/otp.rs exists and contains `pub enum OtpSlotStatus`
    - src/model/otp.rs contains `pub fn get_otp_slot_status() -> Result<OtpState>`
    - src/model/otp.rs contains `const SELECT_OTP: &[u8] = &[0x00, 0xA4, 0x04, 0x00, 0x08, 0xA0, 0x00, 0x00, 0x05, 0x27, 0x20, 0x01, 0x01]`
    - src/model/otp.rs contains `const READ_OTP_STATUS: &[u8] = &[0x00, 0x03, 0x00, 0x00]`
    - src/model/mod.rs contains `pub mod otp;` and `pub otp: Option<otp::OtpState>,`
    - src/model/mock.rs contains `otp: Some(otp::OtpState`
    - src/model/app_state.rs contains `Otp,` in the Screen enum
    - `cargo check` passes with zero errors
  </acceptance_criteria>
  <done>OtpState/OtpSlotStatus types exist, APDU function follows piv.rs pattern, mock fixture has OTP data, Screen::Otp variant exists, all model code compiles</done>
</task>

<task type="auto">
  <name>Task 2: OTP screen widget + dashboard nav_9 wiring + snapshot test</name>
  <files>src/tui/otp.rs, src/tui/mod.rs, src/tui/dashboard.rs</files>
  <read_first>
    - src/tui/piv.rs (pattern for simple slot-status screen — PivScreen structure)
    - src/tui/dashboard.rs (nav_8 pattern for adding nav_9)
    - src/tui/mod.rs (module registration)
    - src/model/otp.rs (types created in Task 1 — OtpState, OtpSlotStatus)
    - src/tui/widgets/popup.rs (ModalScreen + PopupScreen for potential help popup)
  </read_first>
  <action>
    1. Create `src/tui/otp.rs` following the PivScreen pattern:
       - `pub struct OtpScreen { pub otp_state: Option<crate::model::otp::OtpState> }`
       - Constructor: `pub fn new(otp_state: Option<crate::model::otp::OtpState>) -> Self`
       - `impl Widget for OtpScreen`:
         - `widget_type_name` returns `"OtpScreen"`
         - `compose()` returns:
           - `Header::new("OTP Slots")`
           - If otp_state is Some: display slot status labels:
             - `Label::new("OTP Slot Configuration")`
             - `Label::new("")` (spacer)
             - For slot 1: `Label::new("  Slot 1 (short touch): Configured")` or `"  Slot 1 (short touch): Empty"` based on `OtpSlotStatus::Occupied` vs `Empty`. If slot1_touch is true, append " (touch required)"
             - For slot 2: `Label::new("  Slot 2 (long touch): Configured")` or `"  Slot 2 (long touch): Empty"` same pattern
             - `Label::new("")` (spacer)
             - `Label::new("Note: Credential type (Yubico OTP, HMAC-SHA1, static password)")`
             - `Label::new("cannot be read back from hardware — only occupied/empty is detectable.")`
           - If otp_state is None: `Label::new("No YubiKey Detected")` + `Label::new("Insert your YubiKey and press R to refresh.")`
           - `Footer` at the end
         - `key_bindings()` returns static array with: Esc=back, R=refresh, Q=back(hidden)
         - `on_action()`: "back" => `ctx.pop_screen_deferred()`, "refresh" => `ctx.pop_screen_deferred()`
         - `render()`: empty (compose handles layout)

    2. In `src/tui/mod.rs`:
       - Add `pub mod otp;` after the existing `pub mod oath;` line

    3. In `src/tui/dashboard.rs`:
       - Add nav_9 keybinding to DASHBOARD_BINDINGS static array after nav_8:
         ```
         KeyBinding { key: KeyCode::Char('9'), modifiers: KeyModifiers::NONE, action: "nav_9", description: "", show: false },
         ```
       - Update the nav_1 binding description from `"1-8 Navigate"` to `"1-9 Navigate"`
       - Add `"[9] OTP Slots"` Button in compose() after the `[8] FIDO2` button
       - Add button match arm in on_event: `"[9] OTP Slots" => "nav_9"`
       - Add nav_9 action in on_action:
         ```rust
         "nav_9" => {
             let otp_state = self.app_state.yubikey_state()
                 .and_then(|yk| yk.otp.clone());
             ctx.push_screen_deferred(Box::new(crate::tui::otp::OtpScreen::new(otp_state)));
         }
         ```

    4. Add snapshot tests in `src/tui/otp.rs`:
       - `#[cfg(test)] mod tests` with `use textual_rs::TestApp;`
       - `otp_default_state`: create OtpScreen with mock otp state (Slot1 Occupied, Slot2 Empty), 80x24 TestApp, settle, `insta::assert_display_snapshot!(app.backend())`
       - `otp_no_yubikey`: create OtpScreen with None, 80x24 TestApp, settle, snapshot

    5. Run `cargo test` to ensure everything compiles. Run `cargo insta review` or `cargo insta accept` to accept new snapshots if needed.
  </action>
  <verify>
    <automated>cd /Users/michael/code/yubitui && cargo test otp -- --nocapture 2>&1 | head -30 && cargo test dashboard -- --nocapture 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/otp.rs exists and contains `impl Widget for OtpScreen`
    - src/tui/otp.rs contains `"OTP Slots"` in Header
    - src/tui/otp.rs contains `"cannot be read back from hardware"` (honest limitation note)
    - src/tui/mod.rs contains `pub mod otp;`
    - src/tui/dashboard.rs contains `"nav_9"` action and `"[9] OTP Slots"` button label
    - src/tui/dashboard.rs contains `"1-9 Navigate"` in the nav_1 binding description
    - src/tui/dashboard.rs contains `OtpScreen::new(otp_state)` in nav_9 handler
    - `cargo test` passes with zero failures (snapshot tests accepted)
  </acceptance_criteria>
  <done>OTP screen renders slot status from model data, dashboard has [9] OTP Slots button with nav_9 key, snapshot tests pass for populated and empty states</done>
</task>

</tasks>

<verification>
- `cargo check` passes (no compile errors)
- `cargo test model::otp` passes (bitmask parsing test)
- `cargo test tui::otp` passes (snapshot tests)
- `cargo test dashboard` passes (existing dashboard tests not broken)
- In `--mock` mode, pressing 9 from dashboard opens OTP screen showing Slot 1: Configured, Slot 2: Empty
</verification>

<success_criteria>
- OTP screen accessible from dashboard via key 9
- Slot status shows Occupied/Empty (not false credential types)
- Screen displays hardware limitation note about unreadable credential type
- Mock fixture provides deterministic OTP state
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-yubikey-slot-delete-workflow/11-01-SUMMARY.md`
</output>
