---
phase: 11-yubikey-slot-delete-workflow
task: 3
total_tasks: 3
status: paused
last_updated: 2026-03-28T15:17:40.809Z
---

<current_state>
Phase 11, plan 3 of 3 (11-03-PLAN.md) not yet started. But before executing 11-03,
a critical gap was discovered this session: the textual-rs migration built skeleton
screens without wiring the actual YubiKey operations to the model layer.
</current_state>

<completed_work>
- Task 1: 11-01 OTP slot status screen — Done (commit 5856c0ce)
- Task 2: 11-02 Help panels + protocol glossary — Done (commit 4df326bf)
- Session cleanup: deleted dead ratatui-era Action enums (DiagnosticsAction, HelpAction,
  PinAction, PivAction, SshAction, DashboardState) from TUI files
- Session fix: textual-rs Esc bug — run loop missing step 1b, screen key_bindings
  never checked. Fixed in local textual-rs path dep (app.rs).
- Session fix: removed unused Fido2Credential top-level import in fido2.rs
</completed_work>

<remaining_work>
- Task 3: 11-03 onboarding screen + factory default detection — not started
- BLOCKING: Wire operations into existing skeleton screens (see below)
</remaining_work>

<decisions_made>
- textual-rs Esc fix: added missing step 1b block to run loop in
  /Users/michael/code/textual-rs/crates/textual-rs/src/app.rs after the
  focused-widget bindings check. Mirrors existing code in handle_key_event().
- Did NOT add #[allow(dead_code)] to model op files — warnings are evidence
  of unbuilt features that must be fixed, not suppressed.
</decisions_made>

<blockers>
CRITICAL — Operations never wired after textual-rs migration:

| Screen | Operations missing |
|---|---|
| SSH Wizard | All 5 steps: check_ssh_support, enable_ssh_support, configure_shell_ssh, restart_gpg_agent, test_ssh_connection |
| Key Management | generate_key_batch, import_key_programmatic, get_key_attributes, get_ssh_public_key_text |
| Touch Policy | set_touch_policy (APDU) — UI collects slot + admin PIN but never applies it |
| PIN Management | change_user_pin_programmatic, change_admin_pin_programmatic, set_reset_code_programmatic, unblock_user_pin_programmatic, factory_reset_openpgp |
| OATH | get_oath_state (initial load from card), calculate_all (refresh codes) |

OATH add/delete are already wired and work correctly.
</blockers>

<context>
User confirmed all unwired operations must be working. Previous sessions falsely marked
phases complete based on screen existence, not operational correctness. The model layer
has complete, correct implementations of all these operations — the gap is purely in the
TUI on_action handlers not calling them and not handling the async results.

Pattern to follow for wiring: run_worker / run_worker_with_progress in textual-rs,
with on_event handling WorkerResult to display outcome. See fido2.rs ResetGuidanceScreen
and oath.rs DeleteConfirmScreen for working examples of this pattern.

textual-rs is now on crates.io at 0.3.1 (same as local dep). Switch Cargo.toml when
a good moment arises.
</context>

<next_action>
Before starting 11-03: decide with user whether to create new phases for operation
wiring or add to existing phase 11. Then wire operations in priority order:
1. PIN operations (most critical for new users)
2. Touch policy set
3. SSH wizard steps
4. Key gen/import
5. OATH refresh (get_oath_state + calculate_all)
</next_action>
