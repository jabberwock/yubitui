# Continue Here — 2026-03-26

## Where We Are
Phase 05-03 (native card protocol cleanup) — partially executing. The gpg keytocard import flow has been heavily reworked and is mostly working. Paused due to context limit.

## What Works Now (verified)
- **SIG slot import ✓** — `cardedit.genkeys.replace_key → n → success` correctly detects key already on card
- **Card status formatting** — now renders on separate lines (was all on one line)
- **Key attributes / SSH export / slot view** — were broken because `get_key_attributes()` was killing scdaemon without restart; fixed
- **Import list** — `list_gpg_keys()` now returns one entry per importable primary key (excludes sec>/sec#)
- **Kill-by-signal detection** — `child.wait()` now checks `ExitStatusExt::signal()` so kill-9 doesn't trigger a retry loop
- **Two-field import form** — "Key Passphrase (blank if none)" + "Admin PIN"
- **gpg state machine** — pre-buffering replaced with proper state machine; handles `cardedit.genkeys.storekeytype`, `keytocard.where`, `passphrase.enter` (key passphrase first, admin PIN second), `keyedit.save.okay`

## ENC Slot — Current Blocker
The ENC subkey (`DB6A9025EB06A61D`, keygrip `3683B0CBBD9DCB921896F70F9FC12626B9AC90A9`) private key material **is not present locally** and **not on the card**. This is pre-existing — the key was never in `~/.gnupg/private-keys-v1.d/` before this session. The `ssb` record in the keybox has a stale card serial reference from a previous failed import.

**The user needs to either:**
1. Restore from a backup (`gpg --import <backup-file>`)
2. Generate a new encryption subkey: `gpg --edit-key DAB13513860C280ED85C6888A123FF7475998643` → `addkey` → RSA encrypt → `save`
3. Then re-run the import in yubitui

Latest ENC log (`/tmp/yubitui-keytocard-DAB13513860C280ED85C6888A123FF7475998643-slot2.log`):
```
SC_OP_FAILURE
gpg: KEYTOCARD failed: No secret key
```
This confirms the private material is absent, not that our code is wrong.

## Code Changes Since Last Commit
**Modified files (all unstaged):**
- `src/yubikey/key_operations.rs` — massive rework of `run_keytocard_session` state machine, `import_key_programmatic`, `parse_subkey_capabilities` (adds keygrip), `list_gpg_keys`
- `src/app.rs` — two-field import form (key passphrase + admin PIN), `execute_key_import` extracts both
- `src/ui/widgets/pin_input.rs` — added `optional` field to `PinInputField`, `set_optional()` method
- `src/ui/keys.rs` — card status multi-line rendering fix
- `src/yubikey/gpg_status.rs` — (check what changed here)
- `.planning/STATE.md` — stale, needs update

## Next Steps
1. **User action first**: restore ENC subkey or generate new one, then test import end-to-end
2. **Code**: once import works for all slots, commit everything with proper message
3. **Then**: continue 05-03 plan (remove ykman/old gpg dependencies, clean Cargo.toml)

## Test Credentials (for testing import)
- Admin PIN: 77113222
- GPG passphrase: Hpvu3sCHWKYdCFQNMzAv
- Key fingerprint: DAB13513860C280ED85C6888A123FF7475998643

## Key State
- Card serial: 26928089 (D2760001240100000006269280890000)
- SIG: ON card ✓ (signature counter = 9)
- ENC: NOT on card, private key missing locally
- AUT: No auth subkey exists for this key

## gpg Keygrips
- Primary/SIG: FF2FEE8F5CECFB408CA7E95E7A1CC6BE05380772
- ENC subkey: 3683B0CBBD9DCB921896F70F9FC12626B9AC90A9 (key file missing)

## Debug Logs
- `/tmp/yubitui-keytocard-DAB13513860C280ED85C6888A123FF7475998643-slot1.log`
- `/tmp/yubitui-keytocard-DAB13513860C280ED85C6888A123FF7475998643-slot2.log`
- `.planning/debug/gpg-edit-key-hang-respawn.md`
- `.planning/debug/multi-broken-features.md`
